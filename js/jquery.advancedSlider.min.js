(function (i){
    function Pb(ba, W){
        function ca(){
            q = s.find(".slides");
            if(a.settings.shuffle){
                var b = q.children();
                q.find(".slide").each(function (e){
                    i(this).attr("data-index", e)
                });
                b = b.sort(function (){
                    return 0.5 - Math.random()
                });
                q.empty().append(b)
            }
            s.find("a[data-rel^='slider-lightbox']").length && s.find("a[data-rel^='slider-lightbox']").each(function (){
                i(this).attr("rel", i(this).attr("data-rel"))
            });
            q.children().each(function (e, d){
                var f = {}, g = a.settings.shuffle ? parseInt(i(d).data("index")) : e;
                f.properties = {};
                x.push(i(this));
                if(i(this).data("image")){
                    f.lazyImage = i(this).data("image");
                    f.lazyImageLoaded = false
                }
                for(var c = 0; c < i(this).children().length; c++){
                    var h = i(this).children()[c];
                    if(i(h).hasClass("thumbnail") || i(h).is("a") && i(h).children(".thumbnail").length)f.thumbnail = i(h).clone();else
                        if(i(h).hasClass("layer")){
                            if(!f.layer)f.layer = [];
                            f.layer.push(i(h))
                        }else f[i(h).attr("class")] = i(h)
                }
                if(!f.caption && i(this).find(".caption").length)f.caption = i(this).find(".caption");
                if(!f.html && i(this).find(".html").length)f.html = i(this).find(".html");
                if(a.settings.xmlSource){
                    f.properties = y[g].properties;
                    y[e] = f
                }else{
                    for(c = 0; c < Na.length; c++){
                        h = Na[c];
                        var j;
                        if(a.settings.slideProperties)if(a.settings.slideProperties[g])j = a.settings.slideProperties[g][h];
                        f.properties[h] || (f.properties[h] = j == undefined ? a.settings[h] : j)
                    }
                    y.push(f)
                }
            });
            if(w){
                q.find(".slide .thumbnail").parent("a").css("display", "none");
                q.find(".slide .thumbnail").css("display", "none");
                window.onload = function (){
                    q.find(".slide .thumbnail").parent("a").remove();
                    q.find(".slide .thumbnail").remove()
                }
            }else{
                q.find(".slide .thumbnail").parent("a").remove();
                q.find(".slide .thumbnail").remove()
            }
            J = y.length;
            Qb();
            b = {type: "sliderInit"};
            i.isFunction(a.settings.sliderInit) && a.settings.sliderInit.call(this, b)
        }

        function Qb(){
            s.addClass(a.settings.skin).css({width: a.settings.width, height: a.settings.height}).focus(function (){
                Ba = true;
                s.addClass("focus")
            }).blur(function (){
                Ba = false;
                s.hasClass("focus") && s.removeClass("focus")
            });
            M = i('<div class="slider-main"></div>').appendTo(s);
            O == "chrome" || O == "safari" ? s.addClass("webkit") : s.addClass(O);
            if(O == "msie")if(parseInt(da.version) == 6)s.addClass("ie6");else
                if(parseInt(da.version) == 7)s.addClass("ie7");else
                    if(parseInt(da.version) == 8)s.addClass("ie8");else parseInt(da.version) == 9 && s.addClass("ie9");
            u = i('<div class="slide-wrapper"></div>').append(q).appendTo(M).hover(function (){
                Y = true;
                if(a.settings.timerAnimation && a.settings.timerToggle && R != "stopped" && !P){
                    var d = s.find(".timer-animation");
                    w ? d.show() : d.stop().animate({opacity: 1}, a.settings.timerFadeDuration)
                }
                if(Ca && a.settings.lightboxIconToggle){
                    d = u.find(".lightbox-icon");
                    w ? d.css("filter", "") : d.stop().animate({opacity: 1}, a.settings.lightboxIconFadeDuration)
                }
                a.settings.pauseSlideshowOnHover && !L && ja();
                pa && a.settings.captionToggle && rb();
                if(a.settings.slideArrows && a.settings.slideArrowsToggle)w ? s.find(".slide-arrows a").stop().show() : s.find(".slide-arrows a").stop().animate({opacity: 1}, a.settings.slideArrowsShowDuration);
                if(a.settings.slideshowControls && a.settings.slideshowControlsToggle)w ? s.find(".slideshow-controls").stop().show() : s.find(".slideshow-controls").stop().animate({opacity: 1}, a.settings.slideshowControlsShowDuration);
                a.settings.autoToggle && sb()
            }, function (){
                Y = false;
                a.settings.autoToggle && tb();
                if(a.settings.timerAnimation && a.settings.timerToggle && R != "stopped" && !P){
                    var d = s.find(".timer-animation");
                    w ? d.hide() : d.stop().animate({opacity: 0}, a.settings.timerFadeDuration)
                }
                if(Ca && a.settings.lightboxIconToggle){
                    d = u.find(".lightbox-icon");
                    w ? d.css("opacity", 0) : d.stop().animate({opacity: 0}, a.settings.lightboxIconFadeDuration)
                }
                a.settings.pauseSlideshowOnHover && !va && !hb && !L && wa();
                pa && a.settings.captionToggle && ib();
                if(a.settings.slideArrows && a.settings.slideArrowsToggle)w ? s.find(".slide-arrows a").stop().hide() : s.find(".slide-arrows a").stop().animate({opacity: 0}, a.settings.slideArrowsHideDuration);
                if(a.settings.slideshowControls && a.settings.slideshowControlsToggle)w ? s.find(".slideshow-controls").stop().hide() : s.find(".slideshow-controls").stop().animate({opacity: 0}, a.settings.slideshowControlsHideDuration)
            });
            a.settings.border || u.css("border", "none");
            a.settings.glow || u.css({
                "box-shadow": "none", "-moz-box-shadow": "none", "-webkit-box-shadow": "none"
            });
            a.settings.effectType == "swipe" && i('<div class="swipeable-container"></div>').appendTo(u).append(q);
            q.find(".slide").each(function (d){
                var f = i(this), g = f.find("img.image");
                f = f.find(".caption");
                var c = y[d], h = c.properties;
                if(g.length){
                    g.data("index", d);
                    c.imagePath = g.attr("src")
                }
                if(f.length){
                    d = parseFloat(h.captionBackgroundOpacity);
                    g = h.captionBackgroundColor;
                    h = i('<div class="caption-container"></div>').appendTo(f.parent());
                    h = i('<div class="wrapper"></div>').appendTo(h);
                    i('<div class="background"></div>').css({opacity: d, "background-color": g}).appendTo(h);
                    f.appendTo(h)
                }
            });
            q.find(".layer.static").each(function (){
                var d = i(this), f = d.data();
                typeof f.width !== "undefined" && d.css("width", f.width);
                typeof f.height !== "undefined" && d.css("height", f.height);
                typeof f.depth !== "undefined" && d.css("z-index", f.depth);
                var g = f.position ? f.position.toLowerCase() : "topleft", c = g.indexOf("right") != -1 ? "right" : "left";
                g = g.indexOf("bottom") != -1 ? "bottom" : "top";
                if(typeof f.horizontal !== "undefined")if(f.horizontal == "left" && c == "left" || f.horizontal == "right" && c == "right")d.css(c, 0);else
                    if(f.horizontal == "right" && c == "left" || f.horizontal == "left" && c == "right"){
                        d.css("margin-" + c, -d.outerWidth(false));
                        d.css(c, "100%")
                    }else
                        if(f.horizontal == "center"){
                            d.css("margin-" + c, -d.outerWidth(false) * 0.5);
                            d.css(c, "50%")
                        }else d.css(c, f.horizontal);else d.css(c, 0);
                if(typeof f.vertical !== "undefined")if(f.vertical == "top" && g == "top" || f.vertical == "bottom" && g == "bottom")d.css(g, 0);else
                    if(f.vertical == "bottom" && g == "top" || f.vertical == "top" && g == "bottom"){
                        d.css("margin-" + g, -d.outerHeight(false));
                        d.css(g, "100%")
                    }else
                        if(f.vertical == "center"){
                            d.css("margin-" + g, -d.outerHeight(false) * 0.5);
                            d.css(g, "50%")
                        }else d.css(g, f.vertical);else d.css(g, 0);
                d.css("visibility", "inherit").addClass("init")
            });
            q.find("video").each(function (){
                var d = i(this), f = d.parent();
                if(Da){
                    var g = d[0].outerHTML;
                    d.remove();
                    f.html(g);
                    d = f.find("video");
                    d[0].load()
                }
                d.hasClass("video-js") && VideoJS(d.attr("id"), d.data("video"))
            });
            q.find(".jw-player").length && q.find(".jw-player").each(function (d){
                video = i(this);
                jwPlayerArgs = {};
                jwPlayerModes = [];
                flashModeSources = [];
                html5ModeSources = [];
                sources = [];
                video.find(".source").each(function (){
                    var g = i(this);
                    if(g.data("quality"))jwPlayerArgs.plugins = {"hd-2": {file: g.data("file")}};else
                        if(g.hasClass("flash"))flashModeSources.push(i(this).data());else g.hasClass("html5") ? html5ModeSources.push(i(this).data()) : sources.push(i(this).data())
                });
                video.find(".source").each(function (){
                    var g = i(this);
                    if(g.hasClass("flash"))flashModeSources.push(i(this).data());else g.hasClass("html5") ? html5ModeSources.push(i(this).data()) : sources.push(i(this).data())
                });
                if(flashModeSources.length){
                    var f = {type: "flash"};
                    f.config = {};
                    f.src = a.settings.jwPlayerPath + "/player.swf";
                    if(flashModeSources.length == 1)f.config.file = flashModeSources[0].file;else f.config.levels = flashModeSources;
                    if(video.data("provider"))f.config.provider = video.data("provider");
                    if(video.data("streamer"))f.config.streamer = video.data("streamer");
                    jwPlayerModes.push(f)
                }
                if(html5ModeSources.length){
                    f = {type: "html5"};
                    f.config = {};
                    if(html5ModeSources.length == 1)f.config.file = html5ModeSources[0].file;else f.config.levels = html5ModeSources;
                    jwPlayerModes.push(f)
                }
                if(sources.length){
                    jwPlayerArgs.flashplayer = a.settings.jwPlayerPath + "/player.swf";
                    if(sources.length == 1)jwPlayerArgs.file = sources[0].file;else jwPlayerArgs.levels = sources;
                    if(video.data("provider"))jwPlayerArgs.provider = video.data("provider");
                    if(video.data("streamer"))jwPlayerArgs.streamer = video.data("streamer")
                }
                if(video.data("width"))jwPlayerArgs.width = video.data("width");
                if(video.data("height"))jwPlayerArgs.height = video.data("height");
                if(video.data("image"))jwPlayerArgs.image = video.data("image");
                if(video.data("poster"))jwPlayerArgs.image = video.data("poster");
                if(video.attr("data-dock"))jwPlayerArgs.dock = video.attr("data-dock") == "true" ? true : false;
                if(a.settings.jwPlayerSkin)jwPlayerArgs.skin = a.settings.jwPlayerSkin;
                if(a.settings.jwPlayerPlugins)jwPlayerArgs.plugins = a.settings.jwPlayerPlugins[d] || a.settings.jwPlayerPlugins;
                if(video.data("sharing")){
                    if(!jwPlayerArgs.plugins)jwPlayerArgs.plugins = {};
                    jwPlayerArgs.plugins["sharing-2"] = {link: video.data("sharing")}
                }
                if(jwPlayerModes.length)jwPlayerArgs.modes = jwPlayerModes;
                jwplayer(video.attr("id")).setup(jwPlayerArgs)
            });
            q.find("video.sublime-video").length && window.sublimevideo && sublimevideo.ready(function (){
                q.find("video.sublime-video").each(function (){
                    sublimevideo.prepare(i(this).attr("id"))
                })
            });
            q.find("a.video").length && q.find("a.video").each(function (){
                var d = i(this), f = d.children("img").attr("width"), g = d.children("img").attr("height");
                i(this).parent();
                var c = i("<a></a>").addClass("video-play-button").appendTo(d).click(function (h){
                    h.preventDefault()
                });
                d.click(function (h){
                    h.preventDefault();
                    c.fadeOut(300);
                    h = d.attr("href");
                    var j;
                    if(h.indexOf("youtube") != -1 || h.indexOf("youtu.be") != -1){
                        j = /(\?|&)v\=([^&]*)(&|$)/;
                        j = h.match(j);
                        var p = j[2], k = "http://www.youtube.com/embed/" + p + "?enablejsapi=1&wmode=opaque&autoplay=1";
                        h = h.split("?")[1].split("&");
                        i.each(h, function (l, n){
                            if(n.indexOf(p) == -1)k += "&" + n
                        });
                        j = i("<iframe></iframe>").attr("src", k).attr("width", f).attr("height", g).attr("class", d.attr("class")).attr("frameborder", 0).insertBefore(d)
                    }else
                        if(h.indexOf("vimeo") != -1){
                            j = /http:\/\/(www\.)?vimeo.com\/(\d+)/;
                            j = h.match(j);
                            p = j[2];
                            k = "http://player.vimeo.com/video/" + p + "?api=1&autoplay=1";
                            if(h = h.split("?")[1]){
                                h = h.split("&");
                                i.each(h, function (l, n){
                                    if(n.indexOf(p) == -1)k += "&" + n
                                })
                            }
                            j = i("<iframe></iframe>").attr("src", k).attr("width", f).attr("height", g).attr("class", d.attr("class")).attr("frameborder", 0).insertBefore(d)
                        }
                    d.addClass("video-hidden-image");
                    if(a.settings.videoPlayAction == "stopSlideshow" && !L)Ea();else a.settings.videoPlayAction == "pauseSlideshow" && !L && ja();
                    if(a.settings.leaveVideoAction == "removeVideo" || Da || O == "msie" && parseInt(da.version) <= 7)ea = j;else ub()
                })
            });
            xa = a.settings.slideStart;
            var b = window.location.hash;
            if(b){
                b = b.substring(1);
                var e = b.split("-");
                e = parseInt(e.pop());
                b = b.replace("-" + e, "");
                if(s.attr("id") == b && e >= 0 && e <= J - 1)xa = e
            }
            a.settings.slideArrows && Rb();
            a.settings.slideButtons && Sb();
            a.settings.shadow && Tb();
            if(a.settings.thumbnailType == "scroller" || a.settings.thumbnailType == "tooltipAndScroller")Ub();
            a.settings.slideshowControls && Vb();
            a.settings.keyboardNavigation && Wb();
            if(a.settings.fullscreenControls && !(Da || O == "msie"))Xb();
            s.find("a[rel^='slider-lightbox']").length && s.find("a[rel^='slider-lightbox']").prettyPhoto({
                social_tools: "",
                deeplinking: false,
                overlay_gallery: false,
                theme: a.settings.lightboxTheme,
                opacity: a.settings.lightboxOpacity,
                autoplay_slideshow: a.settings.lightboxAutoplay,
                slideshow: a.settings.lightboxAutoplayDelay,
                autoplay: a.settings.lightboxVideoAutoplay,
                allow_resize: a.settings.ligthboxAllowResize,
                openLightbox: function (){
                    if(!va){
                        va = true;
                        s.blur();
                        L || ja()
                    }
                },
                callback: function (){
                    if(va){
                        va = false;
                        s.focus();
                        L || wa()
                    }
                }
            });
            if((a.settings.thumbnailType == "scroller" || a.settings.thumbnailType == "tooltipAndScroller") && (a.settings.thumbnailMouseScroll || a.settings.thumbnailTooltip || a.settings.thumbnailScrollbar) || a.settings.autoToggle)i(document).bind("mousemove", function (d){
                fa = d.pageX;
                ga = d.pageY;
                jb = fa + ga
            });
            ha = "none";
            Oa();
            ka(xa);
            i(window).resize(Oa)
        }

        function Oa(){
            var b = 0, e = 0;
            if(a.settings.responsive){
                s.css({width: "100%", maxWidth: a.settings.width, maxHeight: a.settings.height});
                if(a.settings.aspectRatio == -1)a.settings.aspectRatio = a.settings.width / a.settings.height
            }
            a.settings.aspectRatio != -1 && s.css("height", s.width() / a.settings.aspectRatio);
            if(a.settings.thumbnailType == "scroller" || a.settings.thumbnailType == "tooltipAndScroller")if(a.settings.fullscreenThumbnailScrollerOverlay && qa == "on")if(a.settings.thumbnailOrientation == "horizontal")M.css("height", s.height());else a.settings.thumbnailOrientation == "vertical" && M.css("width", s.width());else
                if(!a.settings.thumbnailScrollerOverlay)if(a.settings.thumbnailOrientation == "horizontal")M.css("height", s.height() - v.outerHeight(true));else a.settings.thumbnailOrientation == "vertical" && M.css("width", s.width() - v.outerWidth(true));
            if(a.settings.shadow){
                var d = M.find(".shadow");
                if(qa == "on" && !a.settings.fullscreenShadow || M.width() < 400)d.css("display", "none");else{
                    d.css("display", "block");
                    var f = d.find(".shadow-middle"), g = d.find(".shadow-left").width() + d.find(".shadow-right").width(), c = parseInt(d.css("height")) + parseInt(d.css("margin-top"));
                    d.css("width", M.width());
                    f.css("width", d.width() - g);
                    b = c > b ? c : b
                }
            }
            if(a.settings.slideButtons){
                d = M.find(".slide-buttons");
                c = d.find("buttons-middle");
                f = c.find("buttons-inner");
                g = parseInt(d.css("height")) + parseInt(d.css("margin-top"));
                d.css("left", 0);
                if(a.settings.slideButtonsCenter){
                    c = (c.width() - f.width()) / 2;
                    f.css("left", c)
                }
                if(a.settings.slideButtonsContainerCenter){
                    c = (M.width() - d.width()) / 2;
                    d.css("left", c)
                }
                if(qa == "on" && !a.settings.fullscreenSlideButtons)d.css("display", "none");else{
                    d.css("display", "block");
                    b = g > b ? g : b
                }
            }
            vb = b;
            b += parseInt(u.css("border-top-width")) + parseInt(u.css("border-bottom-width"));
            e += parseInt(u.css("border-left-width")) + parseInt(u.css("border-right-width"));
            u.css({height: M.height() - b, width: M.width() - e});
            wb = u.width();
            xb = u.height();
            q.find(".slide").each(function (h){
                var j = i(this), p = j.find("img.image"), k = y[h].properties.alignType;
                if(p.length)if(p[0].complete || p[0].readyState == "complete")la(p, k);else{
                    p.css("visibility", "hidden");
                    p.load(function (){
                        p.css("visibility", "");
                        la(p, k)
                    })
                }
                if(a.settings.effectType == "swipe"){
                    var l = o != -1 ? o : xa;
                    if(a.settings.swipeOrientation == "horizontal"){
                        q.css("left", -l * (q.width() + a.settings.swipeSlideDistance));
                        j.css("left", h * (q.width() + a.settings.swipeSlideDistance))
                    }else
                        if(a.settings.swipeOrientation == "vertical"){
                            q.css("top", -l * (q.height() + a.settings.swipeSlideDistance));
                            j.css("top", h * (q.height() + a.settings.swipeSlideDistance))
                        }
                }
            });
            if((a.settings.thumbnailType == "tooltipAndScroller" || a.settings.thumbnailType == "scroller") && a.settings.scaleType != "proportionalFit"){
                kb();
                Fa();
                a.settings.thumbnailMouseScroll && Ga()
            }
            b = {type: "doSliderLayout"};
            i.isFunction(a.settings.doSliderLayout) && a.settings.doSliderLayout.call(this, b)
        }

        function la(b, e){
            if(!b.data("init")){
                b.data("init", true);
                b.data("width", b.width());
                b.data("height", b.height());
                b.data("state", "loaded")
            }
            if(!a.settings.allowScaleUp && (b.css("maxWidth") != b.width() || b.css("maxHeight") != b.height()))b.css({
                maxWidth: b.data("width"), maxHeight: b.data("height")
            });
            switch(a.settings.scaleType){
                case "exactFit":
                    b.css({width: "100%", height: "100%"});
                    break;
                case "insideFit":
                    b.width() / b.height() >= u.width() / u.height() ? b.css({
                        width: "100%", height: "auto"
                    }) : b.css({width: "auto", height: "100%"});
                    break;
                case "outsideFit":
                    b.width() / b.height() <= u.width() / u.height() ? b.css({
                        width: "100%", height: "auto"
                    }) : b.css({width: "auto", height: "100%"});
                    break;
                case "proportionalFit":
                    if(b.data("index") == o && (b[0].complete || b[0].readyState == "complete")){
                        u.css("width", wb);
                        u.css("height", xb);
                        b.css({width: "100%", height: "auto"});
                        u.css("height", b.height());
                        M.css("height", u.outerHeight(true) + vb);
                        if(a.settings.thumbnailType == "scroller" || a.settings.thumbnailType == "tooltipAndScroller"){
                            a.settings.thumbnailOrientation == "horizontal" && !a.settings.thumbnailScrollerOverlay ? s.css("height", M.height() + v.outerHeight(true)) : s.css("height", M.height());
                            kb();
                            Fa();
                            a.settings.thumbnailMouseScroll && Ga()
                        }else s.css("height", M.height());
                        if(C != -1){
                            var d = x[C].find("img.image"), f = y[C].properties.alignType;
                            d.length && yb(d, f)
                        }
                    }
            }
            yb(b, e)
        }

        function yb(b, e){
            var d = b.width(), f = b.height();
            switch(e){
                case "leftTop":
                    b.css({left: 0, top: 0});
                    break;
                case "leftCenter":
                    b.css({
                        left: 0, top: "50%", "margin-top": -f * 0.5
                    });
                    break;
                case "leftBottom":
                    b.css({left: 0, bottom: 0});
                    break;
                case "centerTop":
                    b.css({left: "50%", top: 0, "margin-left": -d * 0.5});
                    break;
                case "centerCenter":
                    b.css({left: "50%", top: "50%", "margin-left": -d * 0.5, "margin-top": -f * 0.5});
                    break;
                case "centerBottom":
                    b.css({left: "50%", bottom: 0, "margin-left": -d * 0.5});
                    break;
                case "rightTop":
                    b.css({right: 0, top: 0});
                    break;
                case "rightCenter":
                    b.css({right: 0, top: "50%", "margin-top": -f * 0.5});
                    break;
                case "rightBottom":
                    b.css({right: 0, bottom: 0});
                    break;
                case "default":
                    b.css({
                        left: "50%", top: "50%", "margin-left": -d * 0.5, "margin-top": -f * 0.5
                    })
            }
        }

        function Rb(){
            var b = i('<div class="slide-arrows"></div>').appendTo(u), e = i('<a class="previous"></a>').appendTo(b).click(function (){
                ha = "previousButton";
                ra()
            });
            b = i('<a class="next"></a>').appendTo(b).click(function (){
                ha = "nextButton";
                ma()
            });
            if(a.settings.slideArrowsToggle)if(w){
                e.hide();
                b.hide()
            }else{
                e.css("opacity", 0);
                b.css("opacity", 0)
            }
        }

        function Vb(){
            var b = i('<div class="slideshow-controls"></div>').appendTo(u), e = a.settings.slideshow ? "pause" : "play";
            R = a.settings.slideshow ? "playing" : "stopped";
            b.addClass(e).click(function (){
                if(R == "playing" || R == "paused")Pa();else R == "stopped" && zb()
            });
            if(a.settings.slideshowControlsToggle)w ? b.hide() : b.css("opacity", 0)
        }

        function Sb(){
            var b = J, e = i('<div class="slide-buttons"></div>').appendTo(M);
            i('<div class="left"></div>').appendTo(e);
            var d = i('<div class="middle"></div>').appendTo(e);
            i('<div class="right"></div>').appendTo(e);
            d = i('<div class="buttons-inner"></div>').appendTo(d);
            for(var f = 0; f < b; f++){
                var g = i('<a rel="' + f + '"></a>').appendTo(d);
                a.settings.slideButtonsNumber && i('<div class="number">' + (f + 1) + "</div>").appendTo(g);
                g.hover(function (){
                    var c = parseInt(i(this).attr("rel"));
                    i(this).hasClass("select") || i(this).addClass("over");
                    if(y[c].properties.thumbnailType == "tooltip" || y[c].properties.thumbnailType == "tooltipAndScroller")Yb(c)
                }, function (){
                    var c = parseInt(i(this).attr("rel"));
                    i(this).hasClass("select") || i(this).removeClass("over");
                    if(y[c].properties.thumbnailType == "tooltip" || y[c].properties.thumbnailType == "tooltipAndScroller")Zb()
                });
                g.click(function (){
                    ha = "button";
                    ka(parseInt(i(this).attr("rel")))
                })
            }
            if(a.settings.slideButtonsToggle){
                w ? e.hide() : e.css("opacity", 0);
                s.hover(function (){
                    w ? e.show() : e.stop().animate({opacity: 1}, a.settings.slideButtonsShowDuration)
                }, function (){
                    w ? e.hide() : e.stop().animate({opacity: 0}, a.settings.slideButtonsHideDuration)
                })
            }
        }

        function Wb(){
            i(document).keydown(function (b){
                if(b.which == 37 && (a.settings.keyboardNavigationOnFocusOnly && Ba || !a.settings.keyboardNavigationOnFocusOnly))ra();else
                    if(b.which == 39 && (a.settings.keyboardNavigationOnFocusOnly && Ba || !a.settings.keyboardNavigationOnFocusOnly))ma();else b.which == 13 && Ba && x[o].children("a").length && x[o].children("a").not(".thumbnail")[0].click()
            })
        }

        function Xb(){
            var b = i('<div class="full-screen-controls"></div>').appendTo(u);
            b.click(function (){
                if(qa == "off")if(ba.requestFullScreen)ba.requestFullScreen();else
                    if(ba.mozRequestFullScreen)ba.mozRequestFullScreen();else ba.webkitRequestFullScreen && ba.webkitRequestFullScreen();else
                    if(qa == "on")if(document.cancelFullScreen)document.cancelFullScreen();else
                        if(document.mozCancelFullScreen)document.mozCancelFullScreen();else document.webkitCancelFullScreen && document.webkitCancelFullScreen()
            });
            document.addEventListener("fullscreenchange", lb);
            document.addEventListener("mozfullscreenchange", lb);
            document.addEventListener("webkitfullscreenchange", lb);
            if(a.settings.fullscreenControlsToggle && !Y)b.css("opacity", 0);else w || b.css({opacity: 0}).stop().animate({opacity: 1}, a.settings.fullscreenIconHideDuration);
            a.settings.fullscreenControlsToggle && u.hover(function (){
                w ? b.css("filter", "") : b.stop().animate({opacity: 1}, a.settings.fullscreenIconShowDuration)
            }, function (){
                w ? b.css("opacity", 0) : b.stop().animate({opacity: 0}, a.settings.fullscreenIconHideDuration)
            })
        }

        function lb(){
            qa = document.fullscreen || document.mozFullScreen || document.webkitIsFullScreen ? "on" : "off";
            if(qa == "on"){
                s.css({width: "100%", height: "100%"}).addClass("fullscreen");
                v && a.settings.fullscreenThumbnailScrollerOverlay && v.addClass("overlay")
            }else{
                s.css({width: a.settings.width, height: a.settings.height}).removeClass("fullscreen");
                v && a.settings.fullscreenThumbnailScrollerOverlay && v.removeClass("overlay")
            }
            Oa()
        }

        function Tb(){
            var b = i('<div class="shadow"></div>').appendTo(M);
            i('<div class="shadow-left"></div>').appendTo(b);
            i('<div class="shadow-middle"></div>').appendTo(b);
            i('<div class="shadow-right"></div>').appendTo(b)
        }

        function Yb(b){
            if(y[b].thumbnail){
                var e = y[b].thumbnail.hasClass("thumbnail") && !y[b].thumbnail.is("img"), d = i('<div class="thumbnail-wrapper"></div>').appendTo(s.find(".buttons-inner")), f;
                if(e){
                    f = y[b].thumbnail.clone();
                    f.addClass("thumbnail-content").css({
                        width: a.settings.thumbnailWidth, height: a.settings.thumbnailHeight
                    }).attr("data-index", b).appendTo(d)
                }else{
                    e = y[b].thumbnail.is("a") ? y[b].thumbnail.find("img") : y[b].thumbnail;
                    var g = e.attr("src"), c = e.attr("alt");
                    f = i('<div class="thumbnail"></div>').css({
                        width: a.settings.thumbnailWidth, height: a.settings.thumbnailHeight
                    }).appendTo(d);
                    if(c){
                        e = i('<div class="caption"></div>').appendTo(f);
                        var h = i('<div class="background"></div>').appendTo(e);
                        c = i('<div class="content">' + c + "</div>").appendTo(e);
                        h = h.css("opacity");
                        e.css("height", c.outerHeight(true));
                        a.settings.thumbnailCaptionPosition == "top" ? e.css("top", 0) : e.css("bottom", 0)
                    }
                    i("<img/>").load(function (){
                        f.css("background-image", "url(" + g + ")")
                    }).attr("src", g)
                }
                d.css({width: f.outerWidth(false), height: f.outerHeight(false)});
                e = s.find(".buttons-inner a").eq(b);
                b = e.position().left - (d.outerWidth(true) - e.width()) / 2;
                e = e.position().top - d.outerHeight(true);
                w && d.find("*").not(".background").css("filter", "inherit");
                d.css({
                    left: b, top: e - a.settings.thumbnailSlideAmount, opacity: 0
                });
                if(w){
                    d.animate({top: e, opacity: 1}, a.settings.thumbnailSlideDuration, a.settings.thumbnailSlideEasing);
                    d.find(".caption .background").animate({opacity: h}, a.settings.thumbnailSlideDuration)
                }else d.animate({
                    top: e, opacity: 1
                }, a.settings.thumbnailSlideDuration, a.settings.thumbnailSlideEasing)
            }
        }

        function Zb(){
            var b = s.find(".buttons-inner .thumbnail-wrapper");
            if(b.length){
                var e = b.find(".background");
                w && e.animate({opacity: 0}, a.settings.thumbnailSlideDuration);
                b.animate({
                    top: b.position().top - a.settings.thumbnailSlideAmount, opacity: 0
                }, a.settings.thumbnailSlideDuration, a.settings.thumbnailSlideEasing, function (){
                    b.remove()
                })
            }
        }

        function ka(b){
            if(b != o){
                if(P)if(a.settings.overrideTransition)switch(Ab){
                    case "slide":
                        x[o].stop(false, true);
                        break;
                    case "fade":
                        x[o].stop(false, true);
                        C != -1 && x[C].stop(false, true);
                        break;
                    case "slice":
                        sa.find(".slice").each(function (){
                            clearTimeout(i(this).data("delay"));
                            i(this).stop().remove()
                        });
                        sa.remove();
                        C != -1 && x[C].css({visibility: ""});
                        x[o].css("visibility", "visible");
                        Q()
                }else
                    if(a.settings.effectType != "swipe")return;
                a.settings.slideshow && Ea();
                if(Ca){
                    Ca = false;
                    var e = u.find(".lightbox-icon");
                    w ? e.remove() : e.stop().animate({opacity: 0}, a.settings.lightboxIconFadeDuration, function (){
                        e.remove()
                    })
                }
                if(ea){
                    hb = false;
                    var d = x[o].find(".video-hidden-image");
                    if((a.settings.leaveVideoAction == "removeVideo" || Da || O == "msie" && parseInt(da.version) <= 7) && d.length){
                        d = x[o].find(".video-hidden-image");
                        d.removeClass("video-hidden-image");
                        ea.remove();
                        d.find(".video-play-button").show()
                    }else ea.leaveVideo();
                    ea = null
                }
                P = true;
                C = o;
                o = b;
                if(C != -1){
                    x[C].removeClass("active");
                    x[C].addClass("transition")
                }
                x[o].addClass("active");
                a.settings.effectType == "swipe" ? q.find(".slide").addClass("transition") : x[o].addClass("transition");
                if(o != na)if(pa && a.settings.effectType != "swipe"){
                    pa = false;
                    ib();
                    Ha && clearTimeout(Ha)
                }
                d = y[b];
                var f = x[b].find("img.image");
                if(d.lazyImage && !d.lazyImageLoaded){
                    var g = i('<img class="image"/>').attr("src", y[b].lazyImage);
                    f.attr("alt") && g.attr("alt", f.attr("alt"));
                    f.attr("title") && g.attr("title", f.attr("title"));
                    g.data("index", b);
                    if(f.length){
                        g.prependTo(f.parent());
                        f.remove()
                    }else g.prependTo(x[b]);
                    f = g;
                    d.imagePath = f.attr("src");
                    d.lazyImageLoaded = true
                }
                if(f.length && (f.data("state") != "loaded" || a.settings.scaleType == "proportionalFit")){
                    var c = d.properties.alignType;
                    if(a.settings.effectType == "swipe"){
                        f.css("visibility", "hidden");
                        if(f[0].complete || f[0].readyState == "complete"){
                            f.css("visibility", "");
                            la(f, c)
                        }else{
                            Bb(x[f.data("index")]);
                            f.load(function (){
                                f.css("visibility", "");
                                la(f, c);
                                Qa(x[f.data("index")])
                            }).error(function (){
                                Qa(x[f.data("index")]);
                                P = false
                            })
                        }
                        Ra()
                    }else
                        if(f[0].complete || f[0].readyState == "complete"){
                            la(f, c);
                            Ra()
                        }else{
                            Bb(u);
                            f.load(function (){
                                la(f, c);
                                Qa(u);
                                Ra()
                            }).error(function (){
                                Qa(u);
                                P = false
                            })
                        }
                }else Ra();
                if(a.settings.slideButtons){
                    g = s.find(".buttons-inner");
                    g.find(".select").removeClass("select");
                    g.find("a").eq(b).attr("class", "select")
                }
                if(a.settings.thumbnailType == "scroller" || a.settings.thumbnailType == "tooltipAndScroller"){
                    v.find(".thumbnail.select").removeClass("select");
                    v.find(".thumbnail[data-index=" + b + "]").removeClass("over").addClass("select");
                    if(a.settings.thumbnailSync && !mb){
                        b = Math.floor(b / (Z * F));
                        b != K && S(b)
                    }
                }
                b = {type: "slideOpen", index: o, data: d};
                i.isFunction(a.settings.slideOpen) && a.settings.slideOpen.call(this, b)
            }
        }

        function ma(){
            ka(o == J - 1 ? 0 : o + 1)
        }

        function ra(){
            ka(o == 0 ? J - 1 : o - 1)
        }

        function $b(b){
            var e = [];
            if(!(J <= 1)){
                if(J == 2)if(b == 0)e.push(1);else b == 1 && e.push(0);else
                    if(J >= 3)e.push(b == 0 ? J - 1 : b - 1, b == J - 1 ? 0 : b + 1);
                for(b = 0; b < e.length; b++){
                    var d = e[b], f = y[d];
                    if(f.lazyImage && !f.lazyImageLoaded){
                        var g = x[d].find("img.image"), c = i('<img class="image"/>').attr("src", f.lazyImage), h = f.properties.alignType;
                        g.attr("alt") && c.attr("alt", g.attr("alt"));
                        g.attr("title") && c.attr("title", g.attr("title"));
                        c.data("index", d);
                        if(g.length){
                            c.prependTo(g.parent());
                            g.remove()
                        }else c.prependTo(x[d]);
                        g = c;
                        f.lazyImageLoaded = true;
                        f.imagePath = g.attr("src");
                        g[0].complete || g[0].readyState == "complete" ? la(g, h) : g.load(function (){
                            la(g, h)
                        })
                    }
                }
            }
        }

        function Bb(b){
            if(!b.find(".preloader").length){
                var e = i('<div class="preloader"></div>').hide().appendTo(b), d = (b.outerWidth(true) - e.width()) * 0.5;
                b = (b.outerHeight(true) - e.height()) * 0.5;
                e.fadeIn(300);
                e.css({left: d, top: b})
            }
        }

        function Qa(b){
            b.find(".preloader").stop().remove()
        }

        function oa(b){
            b.preventDefault()
        }

        function Ra(){
            var b = y[o].properties.effectType;
            if(!a.settings.initialEffect && Cb){
                b = "none";
                Cb = false
            }
            if(a.settings.effectType == "swipe")b = "swipe";else
                if(b == "random")b = ya(["slice", "slide", "fade"]);
            Ab = b;
            var e = y[o];
            x[o].hover(function (){
                var f = {type: "slideMouseOver", index: o, data: e};
                i.isFunction(a.settings.slideMouseOver) && a.settings.slideMouseOver.call(this, f)
            }, function (){
                var f = {type: "slideMouseOut", index: o, data: e};
                i.isFunction(a.settings.slideMouseOut) && a.settings.slideMouseOut.call(this, f)
            }).click(function (){
                var f = {type: "slideClick", index: o, data: e};
                i.isFunction(a.settings.slideClick) && a.settings.slideClick.call(this, f)
            });
            var d = {type: "transitionStart", index: o, data: y[o]};
            i.isFunction(a.settings.transitionStart) && a.settings.transitionStart.call(this, d);
            switch(b){
                case "none":
                    ac();
                    break;
                case "fade":
                    Db();
                    break;
                case "slide":
                    bc();
                    break;
                case "swipe":
                    cc();
                    break;
                case "slice":
                    dc();
                    break;
                default:
                    Db()
            }
        }

        function Q(){
            var b = y[o], e = parseInt(b.properties.captionDelay);
            P = false;
            if(a.settings.slideshow && R != "stopped")if(!a.settings.slideshowLoop && o == J - 1)Pa();else{
                Sa();
                if(a.settings.pauseSlideshowOnHover && Y || L)ja()
            }
            u.find(".transition").removeClass("transition");
            if(o != na){
                if(pa && a.settings.effectType == "swipe"){
                    pa = false;
                    ib();
                    Ha && clearTimeout(Ha)
                }
                if(b.caption){
                    pa = true;
                    Ha = setTimeout(function (){
                        if(!a.settings.captionToggle || a.settings.captionToggle && Y)rb()
                    }, e)
                }else na = -1
            }
            a.settings.preloadNearbyImages && $b(o);
            var d = x[o].children('a[rel^="slider-lightbox"]');
            if(d.length && a.settings.lightboxIcon){
                Ca = true;
                e = d.attr("href");
                var f;
                e = e.indexOf("iframe=true") != -1 || e.indexOf("ajax=true") != -1 || e.indexOf("custom=true") != -1 ? "link" : e.indexOf("youtube.com") != -1 || e.indexOf("youtu.be") != -1 || e.indexOf("vimeo.com") != -1 || e.indexOf(".mp4") != -1 || e.indexOf(".ogv") != -1 ? "video" : "image";
                f = i('<div class="lightbox-icon"></div>').appendTo(u);
                if(e == "video")f.addClass("play");else
                    if(e == "link")f.addClass("hyperlink");else e == "image" && f.addClass("magnifying");
                if(a.settings.lightboxIconToggle && !Y)f.css("opacity", 0);else w || f.css({opacity: 0}).stop().animate({opacity: 1}, a.settings.lightboxIconFadeDuration);
                f.click(function (){
                    d.trigger("click")
                })
            }
            C != -1 && x[C].children(".layer").not(".static").each(function (){
                i(this).css("visibility", "").removeClass("init")
            });
            x[o].children(".layer").not(".static, .init").each(function (){
                var g = i(this), c = g.data(), h = {}, j = {};
                typeof c.width !== "undefined" && g.css("width", c.width);
                typeof c.height !== "undefined" && g.css("height", c.height);
                typeof c.depth !== "undefined" && g.css("z-index", c.depth);
                var p = c.position ? c.position.toLowerCase() : "topleft", k = p.indexOf("right") != -1 ? "right" : "left", l = p.indexOf("bottom") != -1 ? "bottom" : "top";
                if(typeof c.horizontal !== "undefined")if(c.horizontal == "left" && k == "left" || c.horizontal == "right" && k == "right")g.css(k, 0);else
                    if(c.horizontal == "right" && k == "left" || c.horizontal == "left" && k == "right"){
                        g.css("margin-" + k, -g.outerWidth(false));
                        g.css(k, "100%")
                    }else
                        if(c.horizontal == "center"){
                            g.css("margin-" + k, -g.outerWidth(false) * 0.5);
                            g.css(k, "50%")
                        }else g.css(k, c.horizontal);else g.css(k, 0);
                if(typeof c.vertical !== "undefined")if(c.vertical == "top" && l == "top" || c.vertical == "bottom" && l == "bottom")g.css(l, 0);else
                    if(c.vertical == "bottom" && l == "top" || c.vertical == "top" && l == "bottom"){
                        g.css("margin-" + l, -g.outerHeight(false));
                        g.css(l, "100%")
                    }else
                        if(c.vertical == "center"){
                            g.css("margin-" + l, -g.outerHeight(false) * 0.5);
                            g.css(l, "50%")
                        }else g.css(l, c.vertical);else g.css(l, 0);
                k == "left" && g.css("left").indexOf("%") != -1 && g.css("left", g.position().left);
                l == "top" && g.css("top").indexOf("%") != -1 && g.css("top", g.position().top);
                p = parseInt(g.css(k));
                var n = parseInt(g.css(l));
                h[k] = p;
                h[l] = n;
                h.opacity = 1;
                j.opacity = 0;
                if(typeof c.transition !== "undefined"){
                    var m = typeof c.offset !== "undefined" ? c.offset : 50;
                    if(c.transition == "left")j[k] = p + (k == "left" ? m : -m);else
                        if(c.transition == "right")j[k] = p + (k == "right" ? -m : m);else
                            if(c.transition == "up")j[l] = n + (l == "top" ? m : -m);else
                                if(c.transition == "down")j[l] = n + (l == "top" ? -m : m)
                }
                O == "msie" && parseInt(da.version) <= 7 ? g.css("visibility", "inherit").css(h) : g.addClass("init").delay(c.delay).css(j).css("visibility", "inherit").animate(h, c.duration, c.easing, function (){
                    if(c.horizontal)if(c.horizontal == "left" && k == "left" || c.horizontal == "right" && k == "right")g.css(k, 0);else
                        if(c.horizontal == "right" && k == "left" || c.horizontal == "left" && k == "right"){
                            g.css("margin-" + k, -g.outerWidth(false));
                            g.css(k, "100%")
                        }else
                            if(c.horizontal == "center"){
                                g.css("margin-" + k, -g.outerWidth(false) * 0.5);
                                g.css(k, "50%")
                            }else g.css(k, c.horizontal);
                    if(c.vertical)if(c.vertical == "top" && l == "top" || c.vertical == "bottom" && l == "bottom")g.css(l, 0);else
                        if(c.vertical == "bottom" && l == "top" || c.vertical == "top" && l == "bottom"){
                            g.css("margin-" + l, -g.outerHeight(false));
                            g.css(l, "100%")
                        }else
                            if(c.vertical == "center"){
                                g.css("margin-" + l, -g.outerHeight(false) * 0.5);
                                g.css(l, "50%")
                            }else g.css(l, c.vertical)
                })
            });
            ub();
            if(a.settings.reachVideoAction == "playVideo"){
                e = x[o].find("a.video");
                e.length && e.trigger("click")
            }
            b = {type: "transitionComplete", index: o, data: b};
            i.isFunction(a.settings.transitionComplete) && a.settings.transitionComplete.call(this, b)
        }

        function ub(){
            if(i.fn.videoController){
                var b, e;
                if(x[o].find("iframe").length){
                    var d = x[o].find("iframe"), f = d.attr("src");
                    if(f.indexOf("enablejsapi=1") != -1){
                        b = d;
                        e = "youtube-video"
                    }else
                        if(f.indexOf("api=1") != -1){
                            b = d;
                            e = "vimeo-video"
                        }
                }else
                    if(x[o].html().indexOf("jw-player") != -1 || x[o].html().indexOf("jwplayer") != -1){
                        b = x[o].find("[id^=video]").first();
                        e = "jw-player-video"
                    }else
                        if(x[o].find(".video-js").length){
                            b = x[o].find(".video-js").first();
                            e = "html5-video-js"
                        }else
                            if(x[o].find(".sublime-video").length){
                                b = x[o].find(".sublime-video").first();
                                e = "html5-sublime-video"
                            }else
                                if(x[o].find("video").length){
                                    b = x[o].find("video").first();
                                    e = "html5-video"
                                }
                if(b)(ea = y[o].videoInstance ? y[o].videoInstance : b.videoController({
                    videoType: e,
                    reachVideoAction: a.settings.reachVideoAction,
                    leaveVideoAction: a.settings.leaveVideoAction,
                    start: function (){
                        if(a.settings.videoPlayAction == "stopSlideshow" && !L)Ea();else a.settings.videoPlayAction == "pauseSlideshow" && !L && ja()
                    },
                    play: function (){
                        hb = true;
                        if(a.settings.videoPlayAction == "stopSlideshow" && !L)Ea();else a.settings.videoPlayAction == "pauseSlideshow" && !L && ja();
                        var g = {type: "videoPlay"};
                        i.isFunction(a.settings.videoPlay) && a.settings.videoPlay.call(this, g)
                    },
                    pause: function (){
                        if(a.settings.videoPauseAction == "startSlideshow" && !L)Sa();else a.settings.videoPauseAction == "resumeSlideshow" && !L && wa();
                        var g = {type: "videoPause"};
                        i.isFunction(a.settings.videoPause) && a.settings.videoPause.call(this, g)
                    },
                    end: function (){
                        if(a.settings.videoEndAction == "resetVideo")ea.stopVideo();else
                            if(a.settings.videoEndAction == "replayVideo")ea.replayVideo();else
                                if(a.settings.videoEndAction == "startSlideshow" && !L)Sa();else
                                    if(a.settings.videoEndAction == "resumeSlideshow" && !L)wa();else a.settings.videoEndAction == "nextSlide" && ma();
                        var g = {type: "videoEnd"};
                        i.isFunction(a.settings.videoEnd) && a.settings.videoEnd.call(this, g)
                    },
                    fullscreenchange: function (){
                        if(O == "msie")if(i("body").hasClass("vjs-full-window")){
                            a.settings.timerAnimation && s.find(".timer-animation").css("visibility", "hidden");
                            a.settings.slideArrows && u.find(".slide-arrows").css("visibility", "hidden");
                            a.settings.slideButtons && s.find(".slide-buttons").css("visibility", "hidden");
                            a.settings.slideshowControls && u.find(".slideshow-controls").css("visibility", "hidden");
                            a.settings.lightboxIcon && u.find(".lightbox-icon").css("visibility", "hidden");
                            if(a.settings.thumbnailType == "scroller" || a.settings.thumbnailType == "tooltipAndScroller")s.find(".thumbnail-scroller").css("visibility", "hidden")
                        }else{
                            a.settings.timerAnimation && s.find(".timer-animation").css("visibility", "visible");
                            a.settings.slideArrows && u.find(".slide-arrows").css("visibility", "visible");
                            a.settings.slideButtons && s.find(".slide-buttons").css("visibility", "visible");
                            a.settings.slideshowControls && u.find(".slideshow-controls").css("visibility", "visible");
                            a.settings.lightboxIcon && u.find(".lightbox-icon").css("visibility", "visible");
                            if(a.settings.thumbnailType == "scroller" || a.settings.thumbnailType == "tooltipAndScroller")s.find(".thumbnail-scroller").css("visibility", "visible")
                        }
                        var g = {type: "videoFullscreenChange"};
                        i.isFunction(a.settings.videoFullscreenChange) && a.settings.videoFullscreenChange.call(this, g)
                    }
                })) && ea.reachVideo()
            }
        }

        function ac(){
            C != -1 && x[C].css("visibility", "");
            x[o].css("visibility", "visible");
            setTimeout(function (){
                Q()
            }, 1)
        }

        function Db(){
            var b = y[o].properties, e = x[o], d, f = parseInt(b.fadeInDuration), g = b.fadeInEasing, c = parseInt(b.fadeOutDuration);
            b = b.fadeOutEasing;
            w && e.find("*").each(function (){
                i(this).hasClass(".caption-container") || i(this).parents(".caption-container").length == 1 || i(this).css("filter", "inherit")
            });
            e.css({visibility: "visible", opacity: 0}).animate({opacity: 1}, f, g, function (){
                e.css({opacity: ""});
                Q()
            });
            if(C != -1){
                d = x[C];
                w && d.find("*").each(function (){
                    i(this).hasClass(".caption-container") || i(this).parents(".caption-container").length == 1 || i(this).css("filter", "inherit")
                });
                d.animate({opacity: 0}, c, b, function (){
                    d.css({visibility: "", opacity: ""})
                })
            }
        }

        function bc(){
            var b = y[o].properties, e = x[o], d, f = b.slideDirection, g = parseInt(b.slideDuration), c = b.slideEasing;
            b = b.slideLoop;
            if(C != -1)d = x[C];
            if(f == "random")f = ya(["autoHorizontal", "autoVertical", "rightToLeft", "leftToRight", "topToBottom", "bottomToTop"]);
            e.css("visibility", "inherit");
            var h = {}, j, p;
            switch(f){
                case "autoHorizontal":
                    j = "left";
                    p = o == 0 && C == J - 1 && b ? u.width() : o == J - 1 && C == 0 && b ? -u.width() : o > C ? u.width() : -u.width();
                    break;
                case "autoVertical":
                    j = "top";
                    p = o == 0 && C == J - 1 && b ? u.height() : o == J - 1 && C == 0 && b ? -u.height() : o > C ? u.height() : -u.height();
                    break;
                case "rightToLeft":
                    j = "left";
                    p = u.width();
                    break;
                case "leftToRight":
                    j = "left";
                    p = -u.width();
                    break;
                case "topToBottom":
                    j = "top";
                    p = -u.height();
                    break;
                case "bottomToTop":
                    j = "top";
                    p = u.height();
                    break;
                default:
                    j = "left";
                    p = o > C ? u.width() : -u.width()
            }
            e.css(j, p);
            h[j] = 0;
            e.animate(h, {
                duration: g, easing: c, complete: function (){
                    e.css({left: "", top: ""});
                    C != -1 && d.css({visibility: "", left: "", top: ""});
                    Q()
                }, step: function (k){
                    d && d.css(j, k - p)
                }
            })
        }

        function cc(){
            if(C == -1){
                q.css("overflow", "visible").bind("dragstart", oa).find(".slide").each(function (g){
                    var c = i(this);
                    if(a.settings.swipeOrientation == "horizontal")c.css({
                        top: 0, left: g * (q.width() + a.settings.swipeSlideDistance), visibility: "visible"
                    });else a.settings.swipeOrientation == "vertical" && c.css({
                        top: g * (q.height() + a.settings.swipeSlideDistance), left: 0, visibility: "visible"
                    })
                });
                a.settings.swipeGrabCursor && q.addClass("grab");
                var b, e, d = false, f = false;
                if(a.settings.swipeTouchDrag && "ontouchstart"in window)f = d = true;
                if(!f && a.settings.swipeMouseDrag)d = true;
                if(a.settings.swipeOrientation == "horizontal"){
                    q.css("left", -xa * (q.width() + a.settings.swipeSlideDistance));
                    d && q.swipe({
                        threshold: a.settings.swipeThreshold,
                        excludedElements: [],
                        allowPageScroll: "vertical",
                        swipeStatus: function (g, c, h, j){
                            if(c == "start"){
                                b = parseInt(q.css("left"));
                                q.is(":animated") && q.stop();
                                g = i(g.target).closest("a");
                                if(g.length){
                                    g.unbind("click", oa).bind("click", oa);
                                    g.has("rel") && g.unbind("click.prettyphoto")
                                }
                                a.settings.swipeGrabCursor && q.removeClass("grab").addClass("grabbing")
                            }else
                                if(c == "move"){
                                    e = o == J - 1 && h == "left" || o == 0 && h == "right" ? 0.2 : 1;
                                    if(h == "left")q.css("left", b - j * e);else h == "right" && q.css("left", b + j * e)
                                }else
                                    if(c == "cancel"){
                                        q.stop().animate({left: -o * (q.width() + a.settings.swipeSlideDistance)}, a.settings.swipeBackDuration, a.settings.swipeEasing, function (){
                                            P && Q()
                                        });
                                        a.settings.swipeGrabCursor && q.removeClass("grabbing").addClass("grab")
                                    }
                        },
                        click: function (g){
                            g = i(g.target).closest("a");
                            if(g.length){
                                g.unbind("click", oa);
                                g.attr("rel") && i.prettyPhoto && g.bind("click.prettyphoto", i.prettyPhoto.initialize)
                            }
                        },
                        swipeLeft: function (){
                            o < J - 1 ? ma() : q.stop().animate({left: -o * (q.width() + a.settings.swipeSlideDistance)}, a.settings.swipeBackDuration, a.settings.swipeEasing, function (){
                                P && Q()
                            });
                            a.settings.swipeGrabCursor && q.removeClass("grabbing").addClass("grab")
                        },
                        swipeRight: function (){
                            o > 0 ? ra() : q.stop().animate({left: -o * (q.width() + a.settings.swipeSlideDistance)}, a.settings.swipeBackDuration, a.settings.swipeEasing, function (){
                                P && Q()
                            });
                            a.settings.swipeGrabCursor && q.removeClass("grabbing").addClass("grab")
                        }
                    })
                }else
                    if(a.settings.swipeOrientation == "vertical"){
                        q.css("top", -xa * (q.height() + a.settings.swipeSlideDistance));
                        d && q.swipe({
                            threshold: a.settings.swipeThreshold,
                            excludedElements: [],
                            allowPageScroll: "horizontal",
                            swipeStatus: function (g, c, h, j){
                                if(c == "start"){
                                    b = parseInt(q.css("top"));
                                    q.is(":animated") && q.stop();
                                    g = i(g.target).closest("a");
                                    if(g.length){
                                        g.unbind("click", oa).bind("click", oa);
                                        g.has("rel") && g.unbind("click.prettyphoto")
                                    }
                                    a.settings.swipeGrabCursor && q.removeClass("grab").addClass("grabbing")
                                }else
                                    if(c == "move"){
                                        e = o == J - 1 && h == "up" || o == 0 && h == "down" ? 0.2 : 1;
                                        if(h == "up")q.css("top", b - j * e);else h == "down" && q.css("top", b + j * e)
                                    }else
                                        if(c == "cancel"){
                                            q.stop().animate({top: -o * (q.height() + a.settings.swipeSlideDistance)}, a.settings.swipeBackDuration, a.settings.swipeEasing, function (){
                                                P && Q()
                                            });
                                            a.settings.swipeGrabCursor && q.removeClass("grabbing").addClass("grab")
                                        }
                            },
                            click: function (g){
                                g = i(g.target).closest("a");
                                if(g.length){
                                    g.unbind("click", oa);
                                    g.attr("rel") && i.prettyPhoto && g.bind("click.prettyphoto", i.prettyPhoto.initialize)
                                }
                            },
                            swipeUp: function (){
                                o < J - 1 ? ma() : q.stop().animate({top: -o * (q.height() + a.settings.swipeSlideDistance)}, a.settings.swipeBackDuration, a.settings.swipeEasing, function (){
                                    P && Q()
                                });
                                a.settings.swipeGrabCursor && q.removeClass("grabbing").addClass("grab")
                            },
                            swipeDown: function (){
                                o > 0 ? ra() : q.stop().animate({top: -o * (q.height() + a.settings.swipeSlideDistance)}, a.settings.swipeBackDuration, a.settings.swipeEasing, function (){
                                    P && Q()
                                });
                                a.settings.swipeGrabCursor && q.removeClass("grabbing").addClass("grab")
                            }
                        })
                    }
                Q()
            }else
                if(a.settings.swipeOrientation == "horizontal")q.stop().animate({left: -o * (q.width() + a.settings.swipeSlideDistance)}, a.settings.swipeDuration, a.settings.swipeEasing, function (){
                    if(i.support.transition && !q.is(":animated"))Q();else i.support.transition || Q()
                });else a.settings.swipeOrientation == "vertical" && q.stop().animate({top: -o * (q.height() + a.settings.swipeSlideDistance)}, a.settings.swipeDuration, a.settings.swipeEasing, function (){
                    if(i.support.transition && !q.is(":animated"))Q();else i.support.transition || Q()
                })
        }

        function dc(){
            var b = y[o], e = b.properties, d = e.sliceEffectType, f = e.htmlDuringTransition, g = parseInt(e.horizontalSlices), c = parseInt(e.verticalSlices), h = e.slicePattern, j = e.slicePoint, p = e.sliceStartPosition, k = parseFloat(e.sliceStartRatio), l = parseInt(e.sliceDuration), n = e.sliceEasing, m = parseInt(e.sliceDelay), t = e.sliceFade == true || effectType == "fade" ? 0 : 1, z = e.fadePreviousSlide, B = parseInt(e.fadePreviousSlideDuration), A = Math.floor(u.width() / g), H = Math.floor(u.height() / c);
            x[o].find("img.image").clone();
            var Ta = x[o].clone(), ta = [];
            if(C != -1 && z == true){
                var Ua = x[C];
                w && Ua.find("*").each(function (){
                    i(this).hasClass(".caption-container") || i(this).parents(".caption-container").length == 1 || i(this).css("filter", "inherit")
                });
                Ua.animate({opacity: 0}, B, function (){
                    Ua.css({visibility: "", opacity: ""});
                    w && Ua.find("*").each(function (){
                        i(this).hasClass(".caption-container") || i(this).parents(".caption-container").length == 1 || i(this).css("filter", "")
                    })
                })
            }
            Ta.css("visibility", "visible").find(".caption-container").remove();
            f || Ta.find(".html").remove();
            sa = i('<div class="slice-container"></div>').insertAfter(q);
            e.slideMask && sa.css("overflow", "hidden");
            for(e = 0; e < g; e++)for(f = 0; f < c; f++){
                z = i('<div class="slice"></div>').css({
                    left: e * A, top: f * H, width: A, height: H, opacity: t, display: "none"
                }).data({hPos: e, vPos: f}).appendTo(sa);
                var Va;
                if(b.html || b.imagePath || b.layer)Va = Ta.clone();
                if(Va){
                    Va.css({
                        left: -(e * A),
                        top: -(f * H),
                        width: u.width(),
                        height: u.height(),
                        overflow: "hidden",
                        position: "absolute"
                    });
                    z.html(Va).css("overflow", "hidden")
                }
                w && z.find("*").each(function (){
                    i(this).css("filter", "inherit")
                });
                ta.push(z)
            }
            if(d == "random")d = ya(["scale", "fade", "width", "height", "slide"]);
            if(h == "random")h = ya(["randomPattern", "topToBottom", "bottomToTop", "leftToRight", "rightToLeft", "topLeftToBottomRight", "topRightToBottomLeft", "bottomLeftToTopRight", "bottomRightToTopLeft", "horizontalMarginToCenter", "horizontalCenterToMargin", "marginToCenter", "verticalCenterToMargin", "skipOneTopToBottom", "skipOneBottomToTop", "skipOneLeftToRight", "skipOneRightToLeft", "skipOneHorizontal", "skipOneVertical", "spiralMarginToCenterCW", "spiralMarginToCenterCCW", "spiralCenterToMarginCW", "spiralCenterToMarginCCW"]);
            if(g == 1 && h == "skipOneHorizontal" || c == 1 && h == "skipOneVertical")h = "randomPattern";
            if(j == "random")j = ya(["leftTop", "leftCenter", "leftBottom", "centerTop", "centerCenter", "centerBottom", "rightTop", "rightCenter", "rightBottom"]);
            if(p == "random")p = ya(["left", "right", "top", "bottom", "leftTop", "rightTop", "leftBottom", "rightBottom", "horizontalAlternate", "verticalAlternate"]);
            b = ec(ta, h, g, c);
            Wa = b.length;
            for(e = 0; e < Wa; e++)fc(b[e], e, d, j, p, k, l, n, m)
        }

        function fc(b, e, d, f, g, c, h, j, p){
            var k = {}, l = {}, n = parseInt(b.css("width")), m = parseInt(b.css("height")), t = parseInt(b.css("left")), z = parseInt(b.css("top")), B, A;
            if(d == "scale" || d == "width" || d == "height")switch(f){
                case "centerCenter":
                    A = z + m * 0.5;
                    B = t + n * 0.5;
                    break;
                case "rightCenter":
                    A = z + m * 0.5;
                    B = t + n;
                    break;
                case "leftCenter":
                    A = z + m * 0.5;
                    B = t;
                    break;
                case "centerTop":
                    A = z;
                    B = t + n * 0.5;
                    break;
                case "rightTop":
                    A = z;
                    B = t + n;
                    break;
                case "leftTop":
                    A = z;
                    B = t;
                    break;
                case "centerBottom":
                    A = z + m;
                    B = t + n * 0.5;
                    break;
                case "rightBottom":
                    A = z + m;
                    B = t + n;
                    break;
                case "leftBottom":
                    A = z + m;
                    B = t;
                    break;
                default:
                    A = z + m * 0.5;
                    B = t + n * 0.5
            }else
                if(d == "slide")switch(g){
                    case "left":
                        A = z;
                        B = t - n * c;
                        break;
                    case "right":
                        A = z;
                        B = t + n * c;
                        break;
                    case "top":
                        A = z - m * c;
                        B = t;
                        break;
                    case "bottom":
                        A = z + m * c;
                        B = t;
                        break;
                    case "leftTop":
                        A = z - m * c;
                        B = t - n * c;
                        break;
                    case "rightTop":
                        A = z - m * c;
                        B = t + n * c;
                        break;
                    case "leftBottom":
                        A = z + m * c;
                        B = t - n * c;
                        break;
                    case "rightBottom":
                        A = z + m * c;
                        B = t + n * c;
                        break;
                    case "horizontalAlternate":
                        A = z;
                        B = t + n * c * (e % 2 == 0 ? 1 : -1);
                        break;
                    case "verticalAlternate":
                        A = z + m * c * (e % 2 == 0 ? 1 : -1);
                        B = t;
                        break;
                    default:
                        A = z;
                        B = t - n * c
                }
            switch(d){
                case "fade":
                    l = {opacity: 1};
                    break;
                case "scale":
                    k = {width: 0, height: 0, left: B, top: A};
                    l = {width: n, height: m, left: t, top: z, opacity: 1};
                    break;
                case "width":
                    k = {width: 0, left: B};
                    l = {width: n, left: t, opacity: 1};
                    break;
                case "height":
                    k = {height: 0, top: A};
                    l = {height: m, top: z, opacity: 1};
                    break;
                case "slide":
                    k = {left: B, top: A};
                    l = {left: t, top: z, opacity: 1};
                    break;
                default:
                    l = {opacity: 1}
            }
            var H = setTimeout(function (){
                b.css(k).css("display", "block").data("delay", H).animate(l, h, j, function (){
                    Wa--;
                    if(Wa == 0){
                        sa.find(".slice").each(function (){
                            i(this).remove();
                            w && i(this).find("*").each(function (){
                                i(this).css("filter", "inherit")
                            })
                        });
                        sa.remove();
                        C != -1 && x[C].css({visibility: ""});
                        x[o].css("visibility", "visible");
                        Q()
                    }
                })
            }, e * p)
        }

        function rb(){
            na = o;
            var b = y[na].properties, e = b.captionPosition, d = b.captionSize, f = b.captionWidth, g = b.captionHeight, c = b.captionLeft, h = b.captionTop, j = b.captionShowEffect, p = parseInt(b.captionShowEffectDuration), k = b.captionShowEffectEasing, l = b.captionShowSlideDirection;
            f = e == "custom" ? f : "100%";
            g = e == "custom" ? g : "100%";
            c = e == "custom" ? c : 0;
            h = e == "custom" ? h : 0;
            var n = x[o].find(".caption-container"), m = n.find(".wrapper");
            m.find(".background");
            m.find(".caption");
            parseFloat(b.captionBackgroundOpacity);
            b = b.captionBackgroundColor;
            var t = j == "fade" ? 0 : d;
            switch(e){
                case "left":
                    n.css({width: d, height: g, left: c, top: h});
                    l == "auto" && m.css({
                        width: d, height: g, left: -t, top: 0
                    });
                    break;
                case "right":
                    n.css({width: d, height: g, right: c, top: h});
                    l == "auto" && m.css({width: d, height: g, left: t, top: 0});
                    break;
                case "top":
                    n.css({width: f, height: d, left: c, top: h});
                    l == "auto" && m.css({width: f, height: d, left: 0, top: -t});
                    break;
                case "bottom":
                    n.css({width: f, height: d, left: c, bottom: h});
                    l == "auto" && m.css({width: f, height: d, left: 0, top: t});
                    break;
                case "custom":
                    n.css({width: f, height: g, left: c, top: h});
                    m.css({width: f, height: g, left: 0, top: 0});
                    break;
                case "default":
                    n.css({
                        width: f, height: d, left: c, bottom: h
                    });
                    l == "auto" && m.css({width: f, height: d, left: 0, top: t})
            }
            if(j == "fade")w ? n.css("visibility", "visible") : m.stop().css({opacity: 0}).animate({opacity: 1}, p, k);else{
                w ? n.css("visibility", "visible") : m.stop().css({opacity: 1});
                e == "custom" && n.css({width: f, height: g, left: c, top: h});
                if(l == "topToBottom")m.css({width: f, height: g, left: 0, top: -n.height()});else
                    if(l == "bottomToTop")m.css({width: f, height: g, left: 0, top: n.height()});else
                        if(l == "leftToRight")m.css({
                            width: f, height: g, left: -n.width(), top: 0
                        });else l == "rightToLeft" && m.css({width: f, height: g, left: n.width(), top: 0});
                O == "msie" && da.version == "6.0" && m.css("background-color", b);
                m.stop().animate({top: 0, left: 0}, p, k)
            }
        }

        function ib(){
            if(na != -1){
                var b = x[na].find(".caption-container"), e = b.find(".wrapper");
                e.find(".background");
                e.find(".caption");
                var d = y[na].properties, f = d.captionPosition, g = d.captionHideEffect, c = parseInt(d.captionHideEffectDuration), h = d.captionHideEffectEasing;
                d = d.captionHideSlideDirection;
                if(g == "fade")w ? b.css("visibility", "hidden") : e.stop().animate({opacity: 0}, c, h);else
                    if(d == "topToBottom")e.animate({top: e.height()}, c);else
                        if(d == "bottomToTop")e.animate({top: -e.height()}, c);else
                            if(d == "leftToRight")e.animate({left: e.width()}, c);else
                                if(d == "rightToLeft")e.animate({left: -e.width()}, c);else
                                    if(d == "auto")switch(f){
                                        case "left":
                                            e.stop().animate({left: -e.width()}, c);
                                            break;
                                        case "right":
                                            e.stop().animate({left: e.width()}, c);
                                            break;
                                        case "top":
                                            e.stop().animate({top: -e.height()}, c);
                                            break;
                                        case "bottom":
                                            e.stop().animate({top: e.height()}, c);
                                            break;
                                        case "custom":
                                            e.stop().animate({top: e.height()}, c);
                                            break;
                                        case "default":
                                            e.stop().animate({top: e.height()}, c)
                                    }
            }
        }

        function Sa(){
            var b = y[o].properties.slideshowDelay;
            R = "playing";
            a.settings.timerAnimation && gc(b);
            Xa = 0;
            Ya = (new Date).getTime();
            $ && clearTimeout($);
            $ = setTimeout(function (){
                ha = "slideshow";
                if(a.settings.slideshowDirection == "next")ma();else a.settings.slideshowDirection == "previous" && ra()
            }, b)
        }

        function zb(){
            if(!a.settings.slideshow)a.settings.slideshow = true;
            s.find(".slideshow-controls").removeClass("play").addClass("pause");
            Sa()
        }

        function Pa(){
            s.find(".slideshow-controls").removeClass("pause").addClass("play");
            R = "stopped";
            Ea()
        }

        function Ea(){
            $ && clearTimeout($);
            a.settings.timerAnimation && hc()
        }

        function ja(){
            if(a.settings.slideshow && R == "playing" && !P){
                $ && clearTimeout($);
                R = "paused";
                Xa += (new Date).getTime() - Ya
            }
        }

        function wa(){
            if(a.settings.slideshow && R == "paused" && !P){
                var b = y[o].properties.slideshowDelay;
                R = "playing";
                Ya = (new Date).getTime();
                $ && clearTimeout($);
                $ = setTimeout(function (){
                    if(a.settings.slideshowDirection == "next")ma();else a.settings.slideshowDirection == "previous" && ra()
                }, b - Xa)
            }
        }

        function gc(b){
            var e = document.createElement("canvas"), d = Math.max(a.settings.timerStrokeWidth1, a.settings.timerStrokeWidth2) + a.settings.timerRadius * 2, f = d / 2, g = Math.PI / 180, c = 0, h, j, p = a.settings.timerStrokeOpacity1, k = a.settings.timerStrokeOpacity2, l = za(a.settings.timerStrokeColor1).red, n = za(a.settings.timerStrokeColor1).green, m = za(a.settings.timerStrokeColor1).blue, t = za(a.settings.timerStrokeColor2).red, z = za(a.settings.timerStrokeColor2).green, B = za(a.settings.timerStrokeColor2).blue;
            e.width = e.height = d;
            var A = i('<div class="timer-animation"></div>').css({
                width: d, height: d
            }).appendTo(u);
            i(e).appendTo(A);
            if(a.settings.timerAnimationControls){
                i('<div class="timer-animation-controls"></div>').appendTo(A);
                L ? A.addClass("play") : A.addClass("pause");
                A.click(function (){
                    if(L){
                        if(L){
                            L = false;
                            A.removeClass("play").addClass("pause");
                            a.settings.pauseSlideshowOnHover && Y || wa()
                        }
                    }else{
                        L = true;
                        A.removeClass("pause").addClass("play");
                        a.settings.pauseSlideshowOnHover && Y || ja()
                    }
                })
            }
            if(a.settings.timerToggle && !Y)w ? A.hide() : A.css("opacity", 0);else w || A.css({opacity: 0}).stop().animate({opacity: 1}, a.settings.timerFadeDuration);
            var H;
            if(e.getContext)H = e.getContext("2d");else
                if(typeof G_vmlCanvasManager != "undefined" && w)H = G_vmlCanvasManager.initElement(e).getContext("2d");else return;
            H.beginPath();
            H.lineWidth = a.settings.timerStrokeWidth1;
            H.strokeStyle = "rgba(" + l + ", " + n + ", " + m + ", " + p + ")";
            H.arc(f, f, a.settings.timerRadius, 0, 2 * Math.PI, false);
            H.stroke();
            nb = setInterval(function (){
                if(c <= 360 && !(a.settings.pauseSlideshowOnHover && Y || va || L || R == "paused")){
                    h = (new Date).getTime();
                    j = h - Ya;
                    c = (Xa + j) / b * 360 + 1;
                    if(c > 360)c = 360;
                    e.width = e.width;
                    H.beginPath();
                    H.lineWidth = a.settings.timerStrokeWidth1;
                    H.strokeStyle = "rgba(" + l + ", " + n + ", " + m + ", " + p + ")";
                    H.arc(f, f, a.settings.timerRadius, 0, 2 * Math.PI, false);
                    H.stroke();
                    H.beginPath();
                    H.lineWidth = a.settings.timerStrokeWidth2;
                    H.strokeStyle = "rgba(" + t + ", " + z + ", " + B + ", " + k + ")";
                    H.arc(f, f, a.settings.timerRadius, 0, c * g, false);
                    H.stroke()
                }
            }, 20)
        }

        function hc(){
            nb && clearInterval(nb);
            var b = u.find(".timer-animation");
            if(b)w ? b.remove() : b.stop().animate({opacity: 0}, a.settings.timerFadeDuration, function (){
                b.remove()
            })
        }

        function Ub(){
            v = i('<div class="thumbnail-scroller"></div>').appendTo(s);
            D = i('<div class="visible-container"></div>').appendTo(v);
            I = i('<ul class="container"></ul>').appendTo(D);
            a.settings.thumbnailOrientation == "horizontal" ? v.addClass("horizontal") : v.addClass("vertical");
            a.settings.thumbnailScrollerOverlay && v.addClass("overlay");
            for(var b = 0; b < J; b++)if(y[b].thumbnail && (y[b].properties.thumbnailType == "scroller" || y[b].properties.thumbnailType == "tooltipAndScroller")){
                var e = y[b].thumbnail.hasClass("thumbnail") && !y[b].thumbnail.is("img"), d = i('<li class="thumbnail-wrapper"></li>').appendTo(I);
                if(e){
                    e = y[b].thumbnail;
                    e.addClass("thumbnail-content").css({
                        width: a.settings.thumbnailWidth, height: a.settings.thumbnailHeight
                    }).attr("data-index", b).appendTo(d)
                }else{
                    var f = (e = y[b].thumbnail.is("a")) ? y[b].thumbnail.find("img").removeClass("thumbnail") : y[b].thumbnail, g = f.attr("src"), c = f.attr("alt");
                    f.attr("title");
                    if(e){
                        e = y[b].thumbnail;
                        e.addClass("thumbnail").css({
                            width: a.settings.thumbnailWidth, height: a.settings.thumbnailHeight
                        }).attr("data-index", b).appendTo(d);
                        if(f[0].complete || f[0].readyState == "complete"){
                            f.css("margin-left", (a.settings.thumbnailWidth - f.width()) * 0.5);
                            f.css("margin-top", (a.settings.thumbnailHeight - f.height()) * 0.5);
                            e.css("background-image", "none")
                        }else f.load(function (){
                            i(this).css("margin-left", (a.settings.thumbnailWidth - i(this).width()) * 0.5);
                            i(this).css("margin-top", (a.settings.thumbnailHeight - i(this).height()) * 0.5);
                            i(this).parent().css("background-image", "none")
                        })
                    }else{
                        e = i('<a class="thumbnail"></a>').css({
                            "background-image": "url(" + g + ")",
                            width: a.settings.thumbnailWidth,
                            height: a.settings.thumbnailHeight
                        }).attr("data-index", b).appendTo(d);
                        y[b].thumbnail.attr("title") && e.attr("title", y[b].thumbnail.attr("title"))
                    }
                    if(c && a.settings.thumbnailCaption){
                        f = i('<div class="caption"></div>').appendTo(e);
                        g = i('<div class="background"></div>').appendTo(f);
                        c = i('<div class="content">' + c + "</div>").appendTo(f);
                        f.css("height", c.outerHeight(true));
                        a.settings.thumbnailCaptionPosition == "top" ? f.css({top: 0}) : f.css({bottom: 0});
                        if(a.settings.thumbnailCaptionToggle)if(a.settings.thumbnailCaptionEffect == "slide")a.settings.thumbnailCaptionPosition == "top" ? f.css({top: -f.outerHeight(true)}) : f.css({bottom: -f.outerHeight(true)});else
                            if(w){
                                g.data("opacity", g.css("opacity"));
                                g.css("opacity", 0);
                                c.css("opacity", 0)
                            }else f.css("opacity", 0)
                    }
                    c = d.children('a[rel^="slider-lightbox"]');
                    f = x[b].children('a[rel^="slider-lightbox"]');
                    g = c.length && a.settings.lightboxIcon;
                    var h = f.length && a.settings.thumbnailLightboxIcon;
                    if(g || h){
                        c = g ? c.attr("href") : f.attr("href");
                        c = c.indexOf("iframe=true") != -1 || c.indexOf("ajax=true") != -1 || c.indexOf("custom=true") != -1 ? "link" : c.indexOf("youtube.com") != -1 || c.indexOf("youtu.be") != -1 || c.indexOf("vimeo.com") != -1 ? "video" : "image";
                        f = i('<div class="lightbox-icon"></div>').appendTo(e);
                        if(c == "video")f.addClass("play");else
                            if(c == "link")f.addClass("hyperlink");else c == "image" && f.addClass("magnifying");
                        a.settings.thumbnailLightboxIconToggle && f.css("opacity", 0)
                    }
                }
                d.css({width: e.outerWidth(false), height: e.outerHeight(false)});
                F = a.settings.thumbnailLayers == -1 || a.settings.thumbnailLayers * a.settings.maximumVisibleThumbnails > J ? Math.ceil(J / a.settings.maximumVisibleThumbnails) : a.settings.thumbnailLayers;
                if(a.settings.thumbnailOrientation == "horizontal"){
                    d.css("left", Math.floor(V / F) * d.outerWidth(true));
                    d.css("top", V % F * d.outerHeight(true))
                }else{
                    d.css("left", V % F * d.outerWidth(true));
                    d.css("top", Math.floor(V / F) * d.outerHeight(true))
                }
                e.hover(function (){
                    i(this).hasClass("select") || i(this).addClass("over");
                    var k = parseInt(i(this).data("index"));
                    if(a.settings.thumbnailCaptionToggle){
                        var l = i(this).children(".caption"), n = l.children(".background"), m = l.children(".content"), t = n.data("opacity");
                        if(a.settings.thumbnailCaptionEffect == "slide")a.settings.thumbnailCaptionPosition == "top" ? l.stop().animate({top: 0}, a.settings.thumbnailCaptionShowDuration, a.settings.thumbnailCaptionEasing) : l.stop().animate({bottom: 0}, a.settings.thumbnailCaptionShowDuration, a.settings.thumbnailCaptionEasing);else
                            if(w){
                                n.stop().animate({opacity: t}, a.settings.thumbnailCaptionShowDuration, a.settings.thumbnailCaptionEasing);
                                m.stop().animate({opacity: 1}, a.settings.thumbnailCaptionShowDuration, a.settings.thumbnailCaptionEasing)
                            }else l.stop().animate({opacity: 1}, a.settings.thumbnailCaptionShowDuration, a.settings.thumbnailCaptionEasing)
                    }
                    if(a.settings.thumbnailLightboxIconToggle){
                        l = i(this).children(".lightbox-icon");
                        w ? l.css("filter", "") : l.stop().animate({opacity: 1}, a.settings.lightboxIconFadeDuration)
                    }
                    a.settings.thumbnailTooltip && ic(k);
                    k = {type: "thumbnailMouseOver", index: k};
                    i.isFunction(a.settings.thumbnailMouseOver) && a.settings.thumbnailMouseOver.call(this, k)
                }, function (){
                    i(this).hasClass("select") || i(this).removeClass("over");
                    var k = parseInt(i(this).data("index"));
                    if(a.settings.thumbnailCaptionToggle){
                        var l = i(this).children(".caption"), n = l.children(".background"), m = l.children(".content");
                        if(a.settings.thumbnailCaptionEffect == "slide")a.settings.thumbnailCaptionPosition == "top" ? l.stop().animate({top: -l.outerHeight(true)}, a.settings.thumbnailCaptionHideDuration, a.settings.thumbnailCaptionEasing) : l.stop().animate({bottom: -l.outerHeight(true)}, a.settings.thumbnailCaptionHideDuration, a.settings.thumbnailCaptionEasing);else
                            if(w){
                                n.stop().animate({opacity: 0}, a.settings.thumbnailCaptionHideDuration, a.settings.thumbnailCaptionEasing);
                                m.stop().animate({opacity: 0}, a.settings.thumbnailCaptionHideDuration, a.settings.thumbnailCaptionEasing)
                            }else l.stop().animate({opacity: 0}, a.settings.thumbnailCaptionHideDuration, a.settings.thumbnailCaptionEasing)
                    }
                    if(a.settings.thumbnailLightboxIconToggle){
                        l = i(this).children(".lightbox-icon");
                        w ? l.css("opacity", 0) : l.stop().animate({opacity: 0}, a.settings.lightboxIconFadeDuration)
                    }
                    a.settings.thumbnailTooltip && jc(k);
                    k = {type: "thumbnailMouseOut", index: k};
                    i.isFunction(a.settings.thumbnailMouseOut) && a.settings.thumbnailMouseOut.call(this, k)
                });
                if(!a.settings.thumbnailSwipe || w)e.click(function (){
                    var k = i(this).data("index");
                    ha = "thumbnail";
                    var l = {type: "thumbnailClick", index: k};
                    i.isFunction(a.settings.thumbnailClick) && a.settings.thumbnailClick.call(this, l);
                    i(this).attr("href") || ka(k)
                });
                V++
            }
            T = v.find(".thumbnail-wrapper").outerWidth(true);
            U = v.find(".thumbnail-wrapper").outerHeight(true);
            N = Math.ceil(V / (a.settings.maximumVisibleThumbnails * F));
            if(a.settings.thumbnailOrientation == "horizontal"){
                v.css({height: U * F, width: T * a.settings.maximumVisibleThumbnails});
                D.css({height: U * F, width: T * a.settings.maximumVisibleThumbnails})
            }else{
                v.css({width: T * F, height: U * a.settings.maximumVisibleThumbnails});
                D.css({width: T * F, height: U * a.settings.maximumVisibleThumbnails})
            }
            a.settings.thumbnailArrows && kc();
            a.settings.thumbnailButtons && Eb();
            a.settings.thumbnailScrollbar && lc();
            a.settings.thumbnailMouseScroll && Ga();
            a.settings.thumbnailMouseWheel && Fb();
            a.settings.thumbnailTooltip && i('<div class="advanced-slider-tooltip"><div class="content"></div></div>').hide().appendTo(i("body"));
            kb();
            a.settings.thumbnailScrollerToggle && v.css({opacity: 0});
            v.hover(function (){
                mb = true;
                if(a.settings.thumbnailScrollerToggle)w ? v.css("filter", "") : v.stop().animate({opacity: 1}, a.settings.thumbnailScrollerShowDuration)
            }, function (){
                mb = false;
                if(a.settings.thumbnailScrollerToggle)w ? v.css("opacity", 0) : v.stop().animate({opacity: 0}, a.settings.thumbnailScrollerHideDuration)
            });
            if(a.settings.thumbnailSwipe && !w){
                v.bind("dragstart", oa);
                I.addClass("swipe");
                a.settings.thumbnailSwipeGrabCursor && D.addClass("grab");
                var j, p;
                d = b = false;
                if(a.settings.thumbnailSwipeTouchDrag && "ontouchstart"in window)d = b = true;
                if(!d && a.settings.thumbnailSwipeMouseDrag)b = true;
                if(b && a.settings.thumbnailOrientation == "horizontal")D.swipe({
                    threshold: a.settings.thumbnailSwipeThreshold,
                    excludedElements: [],
                    allowPageScroll: "vertical",
                    swipeStatus: function (k, l, n, m){
                        if(l == "start"){
                            j = parseInt(I.css("left"));
                            I.is(":animated") && I.stop();
                            a.settings.thumbnailSwipeGrabCursor && D.removeClass("grab").addClass("grabbing");
                            Ia()
                        }else
                            if(l == "move"){
                                p = K == N - 1 && n == "left" || K == 0 && n == "right" ? 0.2 : 1;
                                if(n == "left")I.css("left", j - m * p);else n == "right" && I.css("left", j + m * p);
                                Aa()
                            }else
                                if(l == "cancel"){
                                    m != 0 && S(K, a.settings.thumbnailSwipeBackDuration);
                                    a.settings.thumbnailSwipeGrabCursor && D.removeClass("grabbing").addClass("grab");
                                    Ja()
                                }else l == "end" && a.settings.thumbnailSwipeGrabCursor && D.removeClass("grabbing").addClass("grab")
                    },
                    click: function (k){
                        k = i(k.target).closest(".thumbnail");
                        if(k.length){
                            var l = k.data("index");
                            ha = "thumbnail";
                            var n = {type: "thumbnailClick", index: l};
                            i.isFunction(a.settings.thumbnailClick) && a.settings.thumbnailClick.call(this, n);
                            k.attr("href") || ka(l)
                        }
                    },
                    swipeLeft: function (){
                        K < N - 1 ? Za() : S(N - 1, a.settings.thumbnailSwipeBackDuration)
                    },
                    swipeRight: function (){
                        K > 0 ? $a() : S(0, a.settings.thumbnailSwipeBackDuration)
                    }
                });else b && a.settings.thumbnailOrientation == "vertical" && D.swipe({
                    threshold: a.settings.thumbnailSwipeThreshold,
                    excludedElements: [],
                    allowPageScroll: "horizontal",
                    swipeStatus: function (k, l, n, m){
                        if(l == "start"){
                            j = parseInt(I.css("top"));
                            I.is(":animated") && I.stop();
                            a.settings.thumbnailSwipeGrabCursor && D.removeClass("grab").addClass("grabbing");
                            Ia()
                        }else
                            if(l == "move"){
                                p = K == N - 1 && n == "up" || K == 0 && n == "down" ? 0.2 : 1;
                                if(n == "up")I.css("top", j - m * p);else n == "down" && I.css("top", j + m * p);
                                Aa()
                            }else
                                if(l == "cancel"){
                                    m != 0 && S(K, a.settings.thumbnailSwipeBackDuration);
                                    a.settings.thumbnailSwipeGrabCursor && D.removeClass("grabbing").addClass("grab");
                                    Ja()
                                }else l == "end" && a.settings.thumbnailSwipeGrabCursor && D.removeClass("grabbing").addClass("grab")
                    },
                    click: function (k){
                        k = i(k.target).closest(".thumbnail");
                        if(k.length){
                            var l = k.data("index");
                            ha = "thumbnail";
                            var n = {type: "thumbnailClick", index: l};
                            i.isFunction(a.settings.thumbnailClick) && a.settings.thumbnailClick.call(this, n);
                            k.attr("href") || ka(l)
                        }
                    },
                    swipeUp: function (){
                        K < N - 1 ? Za() : S(N - 1, a.settings.thumbnailSwipeBackDuration)
                    },
                    swipeDown: function (){
                        K > 0 ? $a() : S(0, a.settings.thumbnailSwipeBackDuration)
                    }
                })
            }
        }

        function kb(){
            v.find(".thumbnail-wrapper").each(function (e){
                var d = i(this);
                d.css({
                    width: d.find(".thumbnail").outerWidth(false), height: d.find(".thumbnail").outerHeight(false)
                });
                T = d.outerWidth(true);
                U = d.outerHeight(true);
                if(a.settings.thumbnailOrientation == "horizontal"){
                    d.css("left", Math.floor(e / F) * d.outerWidth(true));
                    d.css("top", e % F * d.outerHeight(true))
                }else{
                    d.css("left", e % F * d.outerWidth(true));
                    d.css("top", Math.floor(e / F) * d.outerHeight(true))
                }
            });
            if(a.settings.thumbnailOrientation == "horizontal"){
                var b = a.settings.thumbnailArrows ? parseInt(v.css("paddingLeft")) + parseInt(v.css("paddingRight")) : 0;
                aa = Math.ceil(V / F) * T;
                Z = a.settings.thumbnailScrollerResponsive ? Math.max(a.settings.minimumVisibleThumbnails, Math.min(Math.ceil(V / F), a.settings.maximumVisibleThumbnails, Math.floor((u.outerWidth(false) - b) / T))) : Math.min(V, a.settings.maximumVisibleThumbnails);
                v.css("width", T * Z);
                D.css("width", T * Z)
            }else{
                b = a.settings.thumbnailArrows ? parseInt(v.css("paddingTop")) + parseInt(v.css("paddingBottom")) : 0;
                aa = Math.ceil(V / F) * U;
                Z = a.settings.thumbnailScrollerResponsive ? Math.max(a.settings.minimumVisibleThumbnails, Math.min(Math.ceil(V / F), a.settings.maximumVisibleThumbnails, Math.floor((u.outerHeight(false) - b) / U))) : Math.min(V, a.settings.maximumVisibleThumbnails);
                v.css("height", Z * U);
                D.css("height", Z * U)
            }
            if(a.settings.thumbnailScrollerCenter)if(a.settings.thumbnailOrientation == "horizontal")v.css("left", (M.outerWidth(false) - v.outerWidth(false)) / 2);else a.settings.thumbnailOrientation == "vertical" && v.css("top", (u.outerHeight(false) - v.outerHeight(false)) / 2);
            if(Gb != Z){
                Gb = Z;
                N = Math.ceil(V / (Z * F));
                a.settings.thumbnailButtons && a.settings.thumbnailScrollerResponsive && Eb();
                if(a.settings.thumbnailScrollbar && a.settings.thumbnailScrollerResponsive)if(a.settings.thumbnailOrientation == "horizontal"){
                    E.css("width", v.width());
                    ab.css("width", E.width() - thumbnailScrollerScrollbarEdgeSize);
                    E.css("left", (v.outerWidth(false) - E.width()) / 2)
                }else{
                    E.css("height", v.height());
                    ab.css("height", E.height() - thumbnailScrollerScrollbarEdgeSize);
                    E.css("top", (v.outerHeight(false) - E.height()) / 2)
                }
                S(K)
            }
        }

        function kc(){
            ia = i('<div class="arrows"></div>').appendTo(v);
            var b = i('<a class="previous"></a>').click(function (){
                $a()
            }).appendTo(ia), e = i('<a class="next"></a>').click(function (){
                Za()
            }).appendTo(ia);
            if(a.settings.thumbnailOrientation == "horizontal"){
                b.css({top: Math.round((U * F - b.height()) / 2)});
                e.css({top: Math.round((U * F - e.height()) / 2)})
            }else{
                b.css({left: Math.round((T * F - b.width()) / 2)});
                e.css({left: Math.round((T * F - e.width()) / 2)})
            }
            if(a.settings.thumbnailArrowsToggle){
                O == "msie" ? ia.hide() : ia.css("opacity", 0);
                v.hover(function (){
                    O == "msie" ? ia.show() : ia.stop().animate({opacity: 1}, a.settings.thumbnailArrowsShowDuration)
                }, function (){
                    O == "msie" ? ia.hide() : ia.stop().animate({opacity: 0}, a.settings.thumbnailArrowsHideDuration)
                })
            }
        }

        function Eb(){
            if(!G){
                G = i('<div class="buttons"></div>').appendTo(v);
                if(a.settings.thumbnailButtonsToggle){
                    w ? G.hide() : G.css("opacity", 0);
                    v.hover(function (){
                        w ? G.show() : G.stop().animate({opacity: 1}, a.settings.thumbnailButtonsShowDuration)
                    }, function (){
                        w ? G.hide() : G.stop().animate({opacity: 0}, a.settings.thumbnailButtonsShowDuration)
                    })
                }
            }
            if(N == 1)G.empty();else
                if(G.find(".buttons a").length != N && N > 1){
                    G.empty();
                    for(var b, e = 0; e < N; e++){
                        var d = i('<a rel="' + e + '"></a>').appendTo(G);
                        if(a.settings.thumbnailOrientation == "horizontal"){
                            b = d.outerWidth(true);
                            d.css("left", e * b)
                        }else{
                            b = d.outerHeight(true);
                            d.css("top", e * b)
                        }
                        d.bind({
                            mouseover: function (){
                                i(this).hasClass("select") || i(this).addClass("over")
                            }, mouseout: function (){
                                i(this).hasClass("select") || i(this).removeClass("over")
                            }, click: function (){
                                var f = parseInt(i(this).attr("rel"));
                                S(f)
                            }
                        })
                    }
                    if(a.settings.thumbnailOrientation == "horizontal"){
                        G.css("top", U * F);
                        G.css("width", b * N);
                        G.css("height", b);
                        v.css("height", Math.max(v.height(), G.position().top + G.outerHeight(true)))
                    }else{
                        G.css("left", T * F);
                        G.css("height", b * N);
                        G.css("width", b);
                        v.css("width", Math.max(v.width(), G.position().left + G.outerWidth(true)))
                    }
                    if(K > N - 1)K = N - 1;
                    G.find("a").eq(K).addClass("select")
                }
            a.settings.thumbnailOrientation == "horizontal" ? G.css("left", (v.outerWidth(false) - G.outerWidth(false)) / 2) : G.css("top", (v.outerHeight(false) - G.outerHeight(false)) / 2)
        }

        function Za(){
            if(K < N - 1){
                K++;
                S(K)
            }else S(0)
        }

        function $a(){
            if(K > 0){
                K--;
                S(K)
            }else S(N - 1)
        }

        function S(b, e){
            var d = a.settings.thumbnailOrientation == "horizontal" ? D.width() : D.height(), f = b * d;
            K = b;
            if(K == N - 1)f = aa - d;
            Ia();
            I.animate(a.settings.thumbnailOrientation == "horizontal" ? {left: -f} : {top: -f}, {
                duration: e || a.settings.thumbnailScrollDuration,
                easing: a.settings.thumbnailScrollEasing,
                step: function (){
                    Aa()
                },
                complete: function (){
                    Ja()
                }
            });
            if(a.settings.thumbnailButtons){
                d = v.find(".buttons");
                d.find(".select").removeClass("select");
                d.find("a").eq(b).removeClass("over").addClass("select")
            }
        }

        function sb(){
            Hb = setInterval(function (){
                if(Ib != jb){
                    Ib = jb;
                    if(bb){
                        clearTimeout(ob);
                        bb = false
                    }
                    if(pb){
                        pb = false;
                        if(a.settings.slideArrows && a.settings.slideArrowsToggle)w ? s.find(".slide-arrows a").stop().show() : s.find(".slide-arrows a").stop().animate({opacity: 1}, a.settings.slideArrowsShowDuration);
                        if(a.settings.slideshowControls && a.settings.slideshowControlsToggle)w ? s.find(".slideshow-controls").stop().show() : s.find(".slideshow-controls").stop().animate({opacity: 1}, a.settings.slideshowControlsShowDuration);
                        if(a.settings.slideButtons && a.settings.slideButtonsToggle)w ? s.find(".slide-buttons").stop().show() : s.find(".slide-buttons").stop().animate({opacity: 1}, a.settings.slideButtonsShowDuration)
                    }
                }else
                    if(!bb){
                        bb = true;
                        ob = setTimeout(function (){
                            pb = true;
                            if(a.settings.slideArrows && a.settings.slideArrowsToggle)w ? s.find(".slide-arrows a").stop().hide() : s.find(".slide-arrows a").stop().animate({opacity: 0}, a.settings.slideArrowsHideDuration);
                            if(a.settings.slideshowControls && a.settings.slideshowControlsToggle)w ? s.find(".slideshow-controls").stop().hide() : s.find(".slideshow-controls").stop().animate({opacity: 0}, a.settings.slideshowControlsHideDuration);
                            if(a.settings.slideButtons && a.settings.slideButtonsToggle)w ? s.find(".slide-buttons").stop().hide() : s.find(".slide-buttons").stop().animate({opacity: 0}, a.settings.slideButtonsHideDuration)
                        }, a.settings.autoToggleDelay)
                    }
            }, 500)
        }

        function tb(){
            clearInterval(Hb);
            clearTimeout(ob)
        }

        function Ga(){
            a.settings.thumbnailMouseScroll = true;
            var b = 0, e = false, d = D.offset().top, f = D.offset().left, g = D.width(), c = D.height(), h = a.settings.thumbnailOrientation == "horizontal" ? g : c, j = a.settings.thumbnailOrientation == "horizontal" ? f : d, p = a.settings.thumbnailOrientation == "horizontal" ? "left" : "top", k = parseInt(I.css(p));
            cb = setInterval(function (){
                if(!e){
                    e = true;
                    d = D.offset().top;
                    f = D.offset().left;
                    g = D.width();
                    c = D.height()
                }
                if(fa > f && fa < f + g && ga > d && ga < d + c){
                    b = Math.round((-((aa - h + 200) * (((a.settings.thumbnailOrientation == "horizontal" ? fa : ga) - j) / h)) + 100 - k) * a.settings.thumbnailMouseScrollSpeed / 100);
                    k += b
                }else
                    if(Math.abs(b) > 0.1){
                        b *= a.settings.thumbnailMouseScrollEase / 100;
                        k += b
                    }else b = 0;
                if(Math.abs(b) > 0){
                    if(k >= 0)k = 0;else
                        if(k <= h - aa)k = h - aa;
                    Aa();
                    I.css(p, k)
                }
            }, 30)
        }

        function Fb(){
            a.settings.thumbnailMouseWheel = true;
            var b = 0, e = a.settings.thumbnailMouseWheelReverse ? -1 : 1, d, f = a.settings.thumbnailOrientation == "horizontal" ? "left" : "top", g = a.settings.thumbnailOrientation == "horizontal" ? D.width() : D.height();
            D.bind("mousewheel", function (c, h){
                c.preventDefault();
                if(!db){
                    Ia();
                    db = true;
                    b = d = parseInt(I.css(f));
                    eb = setInterval(function (){
                        if(Math.abs(b - d) > 0.5){
                            d += (b - d) * (a.settings.thumbnailMouseWheelSpeed / 100);
                            Aa();
                            I.css(f, d)
                        }else Ja()
                    }, 30)
                }
                b += e * h * 10;
                if(b >= 0)b = 0;else
                    if(b <= g - aa)b = g - aa
            })
        }

        function lc(){
            function b(){
                k = (a.settings.thumbnailOrientation == "horizontal" ? fa : ga) - ua.offset()[z] - l;
                e()
            }

            function e(){
                if(k < 0)k = 0;else
                    if(k > parseInt(ua.css(t)) - parseInt(p.css(t)))k = parseInt(ua.css(t)) - parseInt(p.css(t));
                Ka && p.css(z, k);
                n = k / (parseInt(ua.css(t)) - parseInt(p.css(t)));
                if(!fb){
                    Ia();
                    fb = true;
                    m = parseInt(I.css(z));
                    qb = setInterval(function (){
                        if(Math.abs(Jb() + n) > 0.0010){
                            var B = (-n * (aa - parseInt(D.css(t))) - m) * a.settings.thumbnailScrollbarEase / 100;
                            m += B;
                            Aa();
                            I.css(z, m)
                        }else fb && Ja()
                    }, 30)
                }
            }

            E = i('<div class="scrollbar"></div>').appendTo(v);
            var d = i('<div class="back"></div>').appendTo(E), f = i('<div class="track"></div>').appendTo(E), g = i('<div class="forward"></div>').appendTo(E), c = i('<div class="track-middle"></div>').appendTo(f), h = i('<div class="track-back"></div>').appendTo(f), j = i('<div class="track-forward"></div>').appendTo(f), p = i('<div class="thumb"></div>').appendTo(f);
            i('<div class="thumb-middle"></div>').appendTo(p);
            i('<div class="thumb-back"></div>').appendTo(p);
            i('<div class="thumb-forward"></div>').appendTo(p);
            var k = 0, l, n = 0, m = 0, t = a.settings.thumbnailOrientation == "horizontal" ? "width" : "height", z = a.settings.thumbnailOrientation == "horizontal" ? "left" : "top";
            E.addClass(a.settings.scrollbarSkin);
            ua = f;
            if(a.settings.thumbnailOrientation == "horizontal"){
                E.addClass("horizontal");
                E.css({top: U * F, left: (v.outerWidth(false) - E.width()) / 2});
                v.css("height", Math.max(v.height(), E.position().top + E.outerHeight(true)))
            }else{
                E.addClass("vertical");
                E.css({left: T * F, top: (v.outerHeight(false) - E.height()) / 2});
                v.css("width", Math.max(v.width(), E.position().left + E.outerWidth(true)))
            }
            if(d.width() == 0 && g.width() == 0){
                ab = c;
                thumbnailScrollerScrollbarEdgeSize = a.settings.thumbnailOrientation == "horizontal" ? h.width() + j.width() : h.height() + j.height()
            }else{
                ab = f;
                thumbnailScrollerScrollbarEdgeSize = a.settings.thumbnailOrientation == "horizontal" ? d.width() + g.width() : d.height() + g.height()
            }
            if(a.settings.thumbnailScrollbarToggle){
                w ? E.hide() : E.css("opacity", 0);
                v.hover(function (){
                    w ? E.show() : E.stop().animate({opacity: 1}, a.settings.thumbnailScrollbarShowDuration)
                }, function (){
                    w ? E.hide() : E.stop().animate({opacity: 0}, a.settings.thumbnailScrollbarShowDuration)
                })
            }
            p.bind("mousedown", function (B){
                B.preventDefault();
                l = (a.settings.thumbnailOrientation == "horizontal" ? fa : ga) - p.offset()[z];
                Ka = true;
                i(document).bind("mousemove", b)
            });
            i(document).bind("mouseup", function (){
                if(Ka){
                    Ka = false;
                    i(document).unbind("mousemove", b)
                }
            });
            d.bind("click", function (){
                k = parseInt(p.css(z)) - a.settings.scrollbarArrowScrollAmount;
                e()
            });
            g.bind("click", function (){
                k = parseInt(p.css(z)) + a.settings.scrollbarArrowScrollAmount;
                e()
            })
        }

        function Jb(){
            var b = a.settings.thumbnailOrientation == "horizontal" ? "width" : "height";
            return parseFloat(I.css(a.settings.thumbnailOrientation == "horizontal" ? "left" : "top")) / (aa - parseInt(D.css(b)))
        }

        function Fa(){
            cb && clearInterval(cb);
            if(eb){
                db = false;
                clearInterval(eb)
            }
            if(qb){
                fb = false;
                clearInterval(qb)
            }
        }

        function Ia(){
            Fa();
            I && I.is(":animated") && I.stop()
        }

        function Ja(){
            Fa();
            a.settings.thumbnailMouseScroll && Ga()
        }

        function Aa(){
            if(a.settings.thumbnailScrollbar && !Ka){
                var b = ua.find(".thumb"), e = a.settings.thumbnailOrientation == "horizontal" ? "left" : "top", d = a.settings.thumbnailOrientation == "horizontal" ? "width" : "height";
                d = parseInt(ua.css(d)) - parseInt(b.css(d));
                var f = -Jb() * d;
                f >= 0 && f <= d && b.css(e, f)
            }
        }

        function ic(b){
            b = v.find('.thumbnail[data-index="' + b + '"]');
            var e = b.attr("title");
            if(e){
                var d = i("body").find(".advanced-slider-tooltip");
                b.removeAttr("title");
                d.find(".content").html(e);
                var f = d.outerWidth(false) / 2, g = d.outerHeight(false) + parseInt(d.css("marginBottom"));
                d.css({left: fa, top: ga});
                d.stop(false, true).fadeIn(a.settings.tooltipShowDuration);
                i(document).bind("mousemove.tooltip", function (){
                    d.css({left: fa - f, top: ga - g})
                })
            }
        }

        function jc(b){
            var e = i("body").find(".advanced-slider-tooltip"), d = e.find(".content").html();
            b = v.find('.thumbnail[data-index="' + b + '"]');
            if(d){
                b.attr("title", d);
                e && e.stop(false, true).fadeOut(a.settings.tooltipHideDuration, function (){
                    i(document).unbind("mousemove.tooltip");
                    e.css("left", -9999)
                })
            }
        }

        function ec(b, e, d, f){
            var g = [], c, h, j = 0;
            switch(e){
                case "randomPattern":
                    for(d = []; b.length;){
                        j = Math.floor(Math.random() * b.length);
                        d.push(b[j]);
                        b.splice(j, 1)
                    }
                    var p = d.length;
                    for(e = 0; e < p; e++)g[e] = d[e];
                    break;
                case "topToBottom":
                    for(h = 0; h < f; h++)for(c = 0; c < d; c++)g.push(r(b, c, h));
                    break;
                case "bottomToTop":
                    for(h = f - 1; h >= 0; h--)for(c = d - 1; c >= 0; c--)g.push(r(b, c, h));
                    break;
                case "rightToLeft":
                    for(c = d - 1; c >= 0; c--)for(h = f - 1; h >= 0; h--)g.push(r(b, c, h));
                    break;
                case "leftToRight":
                    for(c = 0; c < d; c++)for(h = 0; h < f; h++)g.push(r(b, c, h));
                    break;
                case "topLeftToBottomRight":
                    for(e = 0; e < d + f - 1; e++){
                        h = 0;
                        for(c = e; c >= 0; c--){
                            r(b, c, h) != undefined && g.push(r(b, c, h));
                            h++
                        }
                    }
                    break;
                case "bottomLeftToTopRight":
                    j = d > f ? d : f;
                    for(e = d - 1; e >= 1 - j; e--){
                        c = 0;
                        for(h = e; h <= d - 1; h++){
                            r(b, c, h) != undefined && g.push(r(b, c, h));
                            c++
                        }
                    }
                    break;
                case "topRightToBottomLeft":
                    j = d > f ? d : f;
                    for(e = d - 1; e >= 1 - j; e--){
                        c = e;
                        for(h = 0; h <= f - 1; h++){
                            r(b, c, h) != undefined && g.push(r(b, c, h));
                            c++
                        }
                    }
                    break;
                case "bottomRightToTopLeft":
                    for(e = f + d - 2; e >= 0; e--){
                        h = 0;
                        for(c = e; c >= 0; c--){
                            r(b, c, h) != undefined && g.push(r(b, c, h));
                            h++
                        }
                    }
                    break;
                case "horizontalMarginToCenter":
                    if(d % 2){
                        for(c = 0; c < Math.floor(d / 2); c++)for(h = 0; h < f; h++){
                            g.push(r(b, c, h));
                            g.push(r(b, d - 1 - c, h))
                        }
                        for(e = 0; e < f; e++)g.push(r(b, Math.floor(d / 2), e))
                    }else for(c = 0; c < Math.floor(d / 2); c++)for(h = 0; h < f; h++){
                        g.push(r(b, c, h));
                        g.push(r(b, d - 1 - c, h))
                    }
                    break;
                case "horizontalCenterToMargin":
                    if(d % 2)for(e = 0; e < f; e++)g.push(r(b, Math.floor(d / 2), e));
                    for(c = Math.floor(d / 2) - 1; c >= 0; c--)for(h = 0; h < f; h++){
                        g.push(r(b, c, h));
                        g.push(r(b, d - 1 - c, h))
                    }
                    break;
                case "verticalMarginToCenter":
                    if(f % 2){
                        for(h = 0; h < Math.floor(f / 2); h++)for(c = 0; c < d; c++){
                            g.push(r(b, c, h));
                            g.push(r(b, c, f - 1 - h))
                        }
                        for(e = 0; e < d; e++)g.push(r(b, e, Math.floor(f / 2)))
                    }else for(h = 0; h < Math.floor(f / 2); h++)for(c = 0; c < d; c++){
                        g.push(r(b, c, h));
                        g.push(r(b, c, f - 1 - h))
                    }
                    break;
                case "verticalCenterToMargin":
                    if(f % 2)for(e = 0; e < d; e++)g.push(r(b, e, Math.floor(f / 2)));
                    for(h = Math.floor(f / 2) - 1; h >= 0; h--)for(c = 0; c < d; c++){
                        g.push(r(b, c, h));
                        g.push(r(b, c, f - 1 - h))
                    }
                    break;
                case "skipOneTopToBottom":
                    for(h = 0; h < f; h++){
                        for(c = j; c < d; c += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    j = 1;
                    for(h = 0; h < f; h++){
                        for(c = j; c < d; c += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    break;
                case "skipOneBottomToTop":
                    for(h = f - 1; h >= 0; h--){
                        for(c = j; c < d; c += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    j = 1;
                    for(h = f - 1; h >= 0; h--){
                        for(c = j; c < d; c += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    break;
                case "skipOneLeftToRight":
                    for(c = 0; c < d; c++){
                        for(h = j; h < f; h += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    j = 1;
                    for(c = 0; c < d; c++){
                        for(h = j; h < f; h += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    break;
                case "skipOneRightToLeft":
                    for(c = d - 1; c >= 0; c--){
                        for(h = j; h < f; h += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    j = 1;
                    for(c = d - 1; c >= 0; c--){
                        for(h = j; h < f; h += 2)g.push(r(b, c, h));
                        j == 0 ? j = 1 : j = 0
                    }
                    break;
                case "skipOneVertical":
                    if(f % 2)for(h = 0; h < f; h++){
                        for(c = j; c < d; c += 2){
                            if(h == Math.floor(f / 2)){
                                h++;
                                for(e = 1 - d % 2; e < d; e += 2){
                                    g.push(r(b, e, Math.floor(f / 2)));
                                    r(b, e - 1, Math.floor(f / 2)) != undefined && g.push(r(b, e - 1, Math.floor(f / 2)))
                                }
                            }
                            g.push(r(b, c, h));
                            g.push(r(b, c, f - h - 1))
                        }
                        j == 0 ? j = 1 : j = 0
                    }else for(h = 0; h < f; h++){
                        for(c = j; c < d; c += 2){
                            g.push(r(b, c, h));
                            g.push(r(b, c, f - h - 1))
                        }
                        j == 0 ? j = 1 : j = 0
                    }
                    break;
                case "skipOneHorizontal":
                    if(d % 2)for(c = 0; c < d; c++){
                        for(h = j; h < f; h += 2){
                            if(c == Math.floor(d / 2)){
                                c++;
                                for(e = 1 - f % 2; e < f; e += 2){
                                    g.push(r(b, Math.floor(d / 2), e));
                                    r(b, Math.floor(d / 2), e - 1) != undefined && g.push(r(b, Math.floor(d / 2), e - 1))
                                }
                            }
                            g.push(r(b, c, h));
                            g.push(r(b, d - 1 - c, h))
                        }
                        j == 0 ? j = 1 : j = 0
                    }else for(c = 0; c < d; c++){
                        for(h = j; h < f; h += 2){
                            g.push(r(b, c, h));
                            g.push(r(b, d - 1 - c, h))
                        }
                        j == 0 ? j = 1 : j = 0
                    }
                    break;
                case "spiralMarginToCenterCW":
                    var k = d, l = f, n, m = 0, t = f < d ? f : d;
                    p = Math.floor(t / 2);
                    for(n = 0; n < p; n++){
                        for(c = m++; c < k; c++)g.push(r(b, c, m - 1));
                        k--;
                        for(h = m; h < l; h++)g.push(r(b, d - m, h));
                        l--;
                        for(e = k; e >= d - k; e--)g.push(r(b, e - 1, l));
                        for(j = l - 1; j >= f - l; j--)g.push(r(b, m - 1, j))
                    }
                    if(t % 2){
                        if(t == f)for(c = m++; c < k; c++)g.push(r(b, c, m - 1));
                        if(t == d)for(h = m++; h < l; h++)g.push(r(b, d - m, h))
                    }
                    break;
                case "spiralMarginToCenterCCW":
                    k = d;
                    l = f;
                    m = 0;
                    t = f < d ? f : d;
                    p = Math.floor(t / 2);
                    for(n = 0; n < p; n++){
                        for(h = m++; h < l; h++)g.push(r(b, m - 1, h));
                        l--;
                        for(c = m; c < k; c++)g.push(r(b, c, f - m));
                        k--;
                        for(e = l; e >= f - l; e--)g.push(r(b, k, e - 1));
                        for(j = k - 1; j >= d - k; j--)g.push(r(b, j, m - 1))
                    }
                    if(t % 2){
                        if(t == f)for(c = m++; c < k; c++)g.push(r(b, c, f - m));
                        if(t == d)for(h = m++; h < l; h++)g.push(r(b, m - 1, h))
                    }
                    break;
                case "spiralCenterToMarginCCW":
                    k = d;
                    l = f;
                    m = 0;
                    t = f < d ? f : d;
                    p = Math.floor(t / 2);
                    for(n = 0; n < p; n++){
                        for(c = m++; c < k; c++)g.push(r(b, c, m - 1));
                        k--;
                        for(h = m; h < l; h++)g.push(r(b, d - m, h));
                        l--;
                        for(e = k; e >= d - k; e--)g.push(r(b, e - 1, l));
                        for(j = l - 1; j >= f - l; j--)g.push(r(b, m - 1, j))
                    }
                    if(t % 2){
                        if(t == f)for(c = m++; c < k; c++)g.push(r(b, c, m - 1));
                        if(t == d)for(h = m++; h < l; h++)g.push(r(b, d - m, h))
                    }
                    g.reverse();
                    break;
                case "spiralCenterToMarginCW":
                    k = d;
                    l = f;
                    m = 0;
                    t = f < d ? f : d;
                    p = Math.floor(t / 2);
                    for(n = 0; n < p; n++){
                        for(h = m++; h < l; h++)g.push(r(b, m - 1, h));
                        l--;
                        for(c = m; c < k; c++)g.push(r(b, c, f - m));
                        k--;
                        for(e = l; e >= f - l; e--)g.push(r(b, k, e - 1));
                        for(j = k - 1; j >= d - k; j--)g.push(r(b, j, m - 1))
                    }
                    if(t % 2){
                        if(t == f)for(c = m++; c < k; c++)g.push(r(b, c, f - m));
                        if(t == d)for(h = m++; h < l; h++)g.push(r(b, m - 1, h))
                    }
                    g.reverse();
                    break;
                default:
                    for(d = []; b.length;){
                        j = Math.floor(Math.random() * b.length);
                        d.push(b[j]);
                        b.splice(j, 1)
                    }
                    p = d.length;
                    for(e = 0; e < p; e++)g[e] = d[e]
            }
            return g
        }

        function r(b, e, d){
            return i.grep(b, function (f){
                return f.data("hPos") == e && f.data("vPos") == d
            })[0]
        }

        function ya(b){
            return b[Math.floor(Math.random() * b.length)]
        }

        function za(b){
            var e = parseInt(b.substring(1, 3), 16), d = parseInt(b.substring(3, 5), 16);
            b = parseInt(b.substring(5, 7), 16);
            return {red: e, green: d, blue: b}
        }

        function Kb(b, e, d){
            var f, g = d[0], c = b === gb;
            d[0] = function (){
                if(g){
                    g.apply(window, arguments);
                    if(!c){
                        delete e[f];
                        g = null
                    }
                }
            };
            f = b.apply(window, d);
            e[f] = {args: d, created: Date.now(), cb: g, id: f};
            return f
        }

        function Lb(b, e, d, f){
            var g = d[f];
            if(g){
                var c = b === gb;
                e(g.id);
                if(!c){
                    e = g.args[1];
                    var h = Date.now() - g.created;
                    if(h < 0)h = 0;
                    e -= h;
                    if(e < 0)e = 0;
                    g.args[1] = e
                }
                g.args[0] = function (){
                    if(g.cb){
                        g.cb.apply(window, arguments);
                        if(!c){
                            delete d[f];
                            g.cb = null
                        }
                    }
                };
                g.created = Date.now();
                g.id = b.apply(window, g.args)
            }
        }

        this.settings = i.extend({}, i.fn.advancedSlider.defaults, W);
        var s = i(ba), a = this, o = -1, C = -1, na = -1, y = [], x = [], J = 0, xa = 0, sa = null, P = false, Y = false, Ca = false, va = false, pa = false, hb = false, L = false, $ = 0, nb = 0, R, Xa = 0, Ya = 0, V = 0, Z = 0, Gb = 0, aa, v, D, I, T, U, ia, G, E, ua, ab, K = 0, N = 0, F = 0, mb = false, cb, eb, db = false, qb, Ka = false, fb = false, fa, ga, Ib = 0, jb = 0, Hb, ob = 0, bb = false, pb = false, Cb = true, ha = "", Ha = 0, ea = null, M, u, q, wb, xb, vb = 0, Wa, Ab, X = window.navigator.userAgent.toLowerCase(), Da = X.match(/ipad/i) != null || X.match(/ipod/i) != null || X.match(/iphone/i) != null, mc = /(opera)(?:.*version)?[ \/]([\w.]+)/, nc = /(msie) ([\w.]+)/, oc = /(mozilla)(?:.*? rv:([\w.]+))?/;
        X = /(webkit)[ \/]([\w.]+)/.exec(X) || mc.exec(X) || nc.exec(X) || X.indexOf("compatible") < 0 && oc.exec(X) || [];
        var O = X[1], da = X[2], w = O == "msie" && parseInt(da) < 9, Ba = false, qa = "off", Na = ["alignType", "effectType", "sliceEffectType", "sliceDelay", "sliceDuration", "sliceEasing", "horizontalSlices", "verticalSlices", "slicePattern", "slicePoint", "sliceStartPosition", "sliceStartRatio", "sliceFade", "captionSize", "captionPosition", "captionShowEffectDuration", "captionShowEffectEasing", "captionHideEffectDuration", "captionHideEffectEasing", "captionShowEffect", "captionHideEffect", "captionLeft", "captionTop", "captionWidth", "captionHeight", "captionShowSlideDirection", "captionHideSlideDirection", "captionBackgroundColor", "captionBackgroundOpacity", "slideshowDelay", "slideMask", "slideDirection", "slideDuration", "slideEasing", "slideLoop", "fadeInDuration", "fadeOutDuration", "fadeInEasing", "fadeOutEasing", "fadePreviousSlide", "fadePreviousSlideDuration", "thumbnailType", "captionDelay", "htmlDuringTransition", "jwPlayerPlugins"];
        (function (){
            if(a.settings.xmlSource){
                s.empty();
                i.ajax({
                    type: "GET",
                    url: a.settings.xmlSource,
                    dataType: O == "msie" ? "text" : "xml",
                    success: function (b){
                        var e, d;
                        if(O == "msie"){
                            e = new ActiveXObject("Microsoft.XMLDOM");
                            e.async = false;
                            e.loadXML(b)
                        }else e = b;
                        q = i('<ul class="slides"></ul>').appendTo(s);
                        d = i(e).find("slider");
                        i(e).find("slide").each(function (f){
                            var g = i('<li class="slide"></li>').appendTo(q), c = i(this), h = c.find("image"), j = c.find("thumbnail"), p = c.find("caption"), k = c.find("html"), l = c.find("layer"), n = c.find("link"), m = c.find("thumbnailLink"), t = h.length || c.attr("data-image"), z, B;
                            c.attr("data-image") && g.attr("data-image", c.attr("data-image"));
                            if(t && n.length)z = i("<a></a>").attr("href", n.text()).attr("title", n.attr("title") ? n.attr("title") : "").attr("target", n.attr("target") ? n.attr("target") : "_self").attr("rel", (n.attr("type") == "slider-lightbox" ? n.attr("type") : "") + (n.attr("group") ? "[" + n.attr("group") + "]" : "")).appendTo(g);
                            if(h.length)i("<img/>").attr("src", h.text()).attr("class", "image").attr("alt", h.attr("alt") ? h.attr("alt") : "").attr("title", h.attr("title") ? h.attr("title") : "").appendTo(z ? z : g);
                            if(j.length && m.length)B = i("<a></a>").attr("href", m.text()).attr("title", m.attr("title") ? m.attr("title") : "").attr("target", m.attr("target") ? m.attr("target") : "_self").attr("rel", (m.attr("type") == "slider-lightbox" ? m.attr("type") : "") + (m.attr("group") ? "[" + m.attr("group") + "]" : "")).appendTo(g);
                            if(j.length)i("<img/>").attr("src", j.text()).attr("class", "thumbnail").attr("alt", j.attr("alt") ? j.attr("alt") : "").attr("title", j.attr("title") ? j.attr("title") : "").appendTo(B ? B : g);
                            if(p.length)i("<div></div>").html(p.text()).attr("class", "caption").appendTo(p.attr("parent") == "link" && z ? z : g);
                            k.length && i("<div></div>").html(k.text()).attr("class", "html").appendTo(g);
                            l.length && i.each(l, function (A){
                                A = i(l[A]);
                                var H = i("<div></div>").html(A.text()).attr("class", "layer").appendTo(g);
                                i.each(A[0].attributes, function (Ta, ta){
                                    ta.nodeName == "style" ? H.addClass(ta.nodeValue) : H.attr("data-" + ta.nodeName, ta.nodeValue)
                                })
                            });
                            g.properties = {};
                            for(c = 0; c < Na.length; c++){
                                h = Na[c];
                                g.properties[h] = W.slideProperties && W.slideProperties[f] && W.slideProperties[f][h] ? W.slideProperties[f][h] : i(this).attr(h) ? i(this).attr(h) : W[h] ? W[h] : d.attr(h) ? d.attr(h) : i.fn.advancedSlider.defaults[h]
                            }
                            y.push(g)
                        });
                        ca();
                        b = {type: "xmlLoaded", data: e};
                        i.isFunction(a.settings.xmlLoaded) && a.settings.xmlLoaded.call(this, b)
                    }
                })
            }else ca()
        })();
        this.nextSlide = ma;
        this.previousSlide = ra;
        this.gotoSlide = ka;
        this.startSlideshow = zb;
        this.stopSlideshow = Pa;
        this.pauseSlideshow = ja;
        this.resumeSlideshow = wa;
        this.scrollToThumbnailPage = S;
        this.scrollToNextThumbnailPage = Za;
        this.scrollToPreviousThumbnailPage = $a;
        this.startThumbnailMouseScroll = Ga;
        this.stopThumbnailMouseScroll = function (){
            a.settings.thumbnailMouseScroll = false;
            clearInterval(cb)
        };
        this.startThumbnailMouseWheel = Fb;
        this.stopThumbnailMouseWheel = function (){
            db = a.settings.thumbnailMouseWheel = false;
            clearInterval(eb)
        };
        this.startAutoToggle = sb;
        this.stopAutoToggle = tb;
        this.doSliderLayout = Oa;
        this.getSlideshowState = function (){
            return R
        };
        this.getCurrentIndex = function (){
            return o
        };
        this.getSlideAt = function (b){
            return y[b]
        };
        this.getTriggerType = function (){
            return ha
        };
        this.isTransition = function (){
            return P
        };
        this.totalSlides = function (){
            return J
        };
        this.getSize = function (){
            return {sliderWidth: s.width(), sliderHeight: s.height(), slideWidth: u.width(), slideHeight: u.height()}
        };
        this.destroy = function (){
            Pa();
            Fa();
            i(document).unbind("mousemove");
            i(document).unbind("mousemove.tooltip")
        };
        var La = {}, Ma = {}, Mb = window.setTimeout, gb = window.setInterval, Nb = window.clearTimeout, Ob = window.clearInterval;
        if(Da){
            window.setTimeout = function (){
                return Kb(Mb, La, arguments)
            };
            window.setInterval = function (){
                return Kb(gb, Ma, arguments)
            };
            window.clearTimeout = function (b){
                var e = La[b];
                if(e){
                    delete La[b];
                    Nb(e.id)
                }
            };
            window.clearInterval = function (b){
                var e = Ma[b];
                if(e){
                    delete Ma[b];
                    Ob(e.id)
                }
            };
            window.addEventListener("scroll", function (){
                for(var b in La)Lb(Mb, Nb, La, b);
                for(b in Ma)Lb(gb, Ob, Ma, b)
            })
        }
    }

    i.fn.advancedSlider = function (ba){
        for(var W = [], ca = 0; ca < this.length; ca++){
            if(!this[ca].advancedslider)this[ca].advancedslider = new Pb(this[ca], ba);
            W.push(this[ca].advancedslider)
        }
        return W.length > 1 ? W : W[0]
    };
    i.fn.advancedSlider.defaults = {
        xmlSource: null,
        width: 500,
        height: 300,
        aspectRatio: -1,
        responsive: false,
        skin: "pixel",
        scrollbarSkin: "scrollbar-1",
        alignType: "centerCenter",
        scaleType: "outsideFit",
        allowScaleUp: false,
        preloadNearbyImages: false,
        slideStart: 0,
        shuffle: false,
        shadow: true,
        border: true,
        glow: true,
        slideshow: true,
        slideshowDelay: 5E3,
        slideshowDirection: "next",
        slideshowLoop: true,
        slideshowControls: false,
        slideshowControlsToggle: true,
        slideshowControlsShowDuration: 500,
        slideshowControlsHideDuration: 500,
        pauseSlideshowOnHover: false,
        slideArrows: true,
        slideArrowsToggle: true,
        slideArrowsShowDuration: 500,
        slideArrowsHideDuration: 500,
        slideButtons: true,
        slideButtonsNumber: false,
        slideButtonsToggle: false,
        slideButtonsShowDuration: 500,
        slideButtonsHideDuration: 500,
        slideButtonsCenter: true,
        slideButtonsContainerCenter: true,
        keyboardNavigation: false,
        keyboardNavigationOnFocusOnly: false,
        autoToggle: false,
        autoToggleDelay: 5E3,
        timerAnimation: true,
        timerAnimationControls: true,
        timerFadeDuration: 500,
        timerToggle: false,
        timerRadius: 18,
        timerStrokeColor1: "#000000",
        timerStrokeColor2: "#FFFFFF",
        timerStrokeOpacity1: 0.5,
        timerStrokeOpacity2: 0.7,
        timerStrokeWidth1: 8,
        timerStrokeWidth2: 4,
        lightbox: false,
        lightboxTheme: "pp_default",
        lightboxOpacity: 0.8,
        lightboxIcon: true,
        lightboxIconToggle: false,
        lightboxIconFadeDuration: 500,
        thumbnailLightboxIcon: true,
        thumbnailLightboxIconToggle: true,
        lightboxAutoplay: false,
        lightboxAutoplayDelay: 5E3,
        lightboxVideoAutoplay: true,
        ligthboxAllowResize: true,
        fullscreenControls: false,
        fullscreenControlsToggle: true,
        fullscreenControlsShowDuration: 500,
        fullscreenControlsHideDuration: 500,
        fullscreenThumbnailScrollerOverlay: true,
        fullscreenSlideButtons: false,
        fullscreenShadow: false,
        htmlDuringTransition: true,
        overrideTransition: false,
        initialEffect: true,
        effectType: "random",
        sliceEffectType: "random",
        sliceDelay: 50,
        sliceDuration: 1E3,
        sliceEasing: "swing",
        horizontalSlices: 5,
        verticalSlices: 3,
        slicePattern: "random",
        slicePoint: "centerCenter",
        sliceStartPosition: "left",
        sliceStartRatio: 1,
        slideMask: false,
        sliceFade: true,
        fadePreviousSlide: false,
        fadePreviousSlideDuration: 300,
        slideDirection: "autoHorizontal",
        slideLoop: false,
        slideDuration: 700,
        slideEasing: "swing",
        fadeInDuration: 700,
        fadeOutDuration: 700,
        fadeInEasing: "swing",
        fadeOutEasing: "swing",
        swipeOrientation: "horizontal",
        swipeDuration: 700,
        swipeBackDuration: 300,
        swipeSlideDistance: 10,
        swipeEasing: "swing",
        swipeMouseDrag: true,
        swipeTouchDrag: true,
        swipeGrabCursor: true,
        swipeThreshold: 50,
        thumbnailType: "tooltip",
        thumbnailWidth: 80,
        thumbnailHeight: 50,
        thumbnailSlideAmount: 10,
        thumbnailSlideDuration: 300,
        thumbnailSlideEasing: "swing",
        thumbnailSwipe: false,
        thumbnailSwipeBackDuration: 300,
        thumbnailSwipeMouseDrag: true,
        thumbnailSwipeTouchDrag: true,
        thumbnailSwipeGrabCursor: true,
        thumbnailSwipeThreshold: 50,
        thumbnailScrollerToggle: false,
        thumbnailScrollerResponsive: false,
        thumbnailScrollerCenter: true,
        thumbnailScrollerOverlay: false,
        thumbnailScrollDuration: 1E3,
        thumbnailScrollEasing: "swing",
        maximumVisibleThumbnails: 5,
        minimumVisibleThumbnails: 1,
        thumbnailOrientation: "horizontal",
        thumbnailLayers: 1,
        thumbnailTooltip: false,
        tooltipShowDuration: 300,
        tooltipHideDuration: 300,
        thumbnailCaption: true,
        thumbnailCaptionPosition: "bottom",
        thumbnailCaptionToggle: true,
        thumbnailCaptionEffect: "slide",
        thumbnailCaptionShowDuration: 500,
        thumbnailCaptionHideDuration: 500,
        thumbnailCaptionEasing: "swing",
        thumbnailScrollbar: false,
        thumbnailButtons: false,
        thumbnailArrows: true,
        thumbnailButtonsToggle: false,
        thumbnailArrowsToggle: false,
        thumbnailScrollbarToggle: false,
        scrollbarArrowScrollAmount: 100,
        thumbnailScrollerHideDuration: 500,
        thumbnailScrollerShowDuration: 500,
        thumbnailArrowsHideDuration: 500,
        thumbnailArrowsShowDuration: 500,
        thumbnailButtonsHideDuration: 500,
        thumbnailButtonsShowDuration: 500,
        thumbnailScrollbarHideDuration: 500,
        thumbnailScrollbarShowDuration: 500,
        thumbnailSync: true,
        thumbnailMouseScroll: false,
        thumbnailMouseScrollEase: 90,
        thumbnailMouseScrollSpeed: 10,
        thumbnailMouseWheel: false,
        thumbnailMouseWheelSpeed: 20,
        thumbnailMouseWheelReverse: false,
        thumbnailScrollbarEase: 10,
        captionToggle: false,
        captionDelay: 0,
        captionSize: 70,
        captionBackgroundOpacity: 0.5,
        captionBackgroundColor: "#000000",
        captionShowEffect: "slide",
        captionShowEffectDuration: 500,
        captionShowEffectEasing: "swing",
        captionShowSlideDirection: "auto",
        captionHideEffect: "fade",
        captionHideEffectDuration: 300,
        captionHideEffectEasing: "swing",
        captionHideSlideDirection: "auto",
        captionPosition: "bottom",
        captionLeft: 50,
        captionTop: 50,
        captionWidth: 300,
        captionHeight: 100,
        videoPlayAction: "stopSlideshow",
        videoPauseAction: "none",
        videoEndAction: "startSlideshow",
        reachVideoAction: "none",
        leaveVideoAction: "pauseVideo",
        jwPlayerPath: "js/jw-player/player.swf",
        jwPlayerSkin: null,
        slideProperties: null,
        sliderInit: null,
        slideOpen: null,
        slideClick: null,
        slideMouseOver: null,
        slideMouseOut: null,
        thumbnailClick: null,
        thumbnailMouseOver: null,
        thumbnailMouseOut: null,
        transitionStart: null,
        transitionComplete: null,
        videoPlay: null,
        videoPause: null,
        videoEnd: null,
        videoFullscreenChange: null,
        xmlLoaded: null,
        doSliderLayout: null
    }
})(jQuery);
